# Local render fixtures — locations & regeneration

Owner-approved: 2026-06-10

The render/ΔE regression tests need large external fixtures (DNGs, Adobe
reference TIFFs, the `dng_validate` binary). They are NOT committed. The
canonical local home is **`~/lrt-cinema-fixtures/`** (override with the
`LRT_CINEMA_FIXTURES` env var). The historical `/tmp/dng_out` location is
still honoured as a fallback — but **macOS wipes /tmp**, which is how the
gym end-to-end gate silently went dormant. Resolution logic:
[tests/fixture_paths.py](tests/fixture_paths.py).

## Contents (as of 2026-06-10)

| File | What | Regenerate with |
|---|---|---|
| `DSC_4053.dng` | Real D750 frame (gym scene), Adobe DNG Converter output of `DSC_4053.NEF` (original on the SanDisk drive) | Adobe DNG Converter on the NEF |
| `DSC_4053_dngvalidate.tif` | Gym ground truth: Adobe's reference render, 16-bit sRGB, Camera Standard | `~/lrt-cinema-fixtures/dng_validate -profile "Camera Standard" -16 -tif ~/lrt-cinema-fixtures/DSC_4053_dngvalidate ~/lrt-cinema-fixtures/DSC_4053.dng` |
| `DSC_4053_synth*.dng` | Synthetic flat-patch DNGs (regenerated by the test itself) | `pytest tests/test_synthetic_dng.py` rebuilds them in-place |
| `dng_validate` | Adobe DNG SDK 1.7.1 reference renderer (built 2026-05-26 from the SDK at `/private/tmp/dng_sdk`) | Download Adobe DNG SDK → `cmake -B _build && cmake --build _build --target dng_validate` |
| `evidence-2026-06-07/` | Dead-session experiment images (cyan/blinds diagnostics) — see [docs/archive/research/dead-session-artifacts-2026-06-07.md](docs/archive/research/dead-session-artifacts-2026-06-07.md) | point-in-time; not regenerable |
| `test-articles/*.dng` + `manifest.json` | Pressure-test articles: analytic scenes (ISO-12233-class bars/zone-plate, clip ramps, patches) mosaicked onto the real D750 grid with sensor-accurate clipping — CONSTRUCTION truth (epistemics in `tools/test_articles/fields.py`) | `python3 tools/test_articles/make_articles.py` (needs `DSC_4053_uncomp.dng` + dnglab) |
| `test-articles/renders/` | Per-article × arm 1:1 crops + engine anchors + README | `python3 tools/test_articles/run_pressure.py --crops` |
| `production/calibration/CAL*_4053.*`, `CALEXP*_4053.*` | Single-variable probe pairs (deflicker EV ×4 — done; global Exposure2012 domain — awaiting owner exports) | XMP copies of `production/xmp/DSC_4053.xmp` with one attribute changed (diff-verify!) + NEF copies; owner exports 16-bit sRGB TIFF, no resize, sharpening off |
| `verify-2026-06-10/` | Owner-eyeball flip-stacks + fringe-forensics crops (native res, zero scaling) | `python3 tools/owner_flip_stack.py`, `python3 tools/demosaic_ab_flip.py`, `python3 tools/fringe_forensics.py` |

## Missing / wanted

- `rose.dng` + `rose_dngval_Camera_Standard.tif` — the rose ship-gate scene.
  **Absent since at least 2026-06-10**; the "rose 0.545" number in old docs is
  unreproducible until restored (CLAIMS.md).
- Production sequence subset (NEFs + XMPs + colorist JPGs + fresh LR exports)
  — arrives with the Phase-1b mount-day runbook.

## Required system pieces (not in this dir)

- Adobe DCPs: `/Library/Application Support/Adobe/CameraRaw/CameraProfiles/Camera/Nikon D750/` (installed by Adobe DNG Converter / Lightroom).
- `dnglab` on PATH (`/opt/homebrew/bin/dnglab`) for the uncompressed-clone step.
