---
name: highlight-recovery-tier1-and-stage9-clamp
description: Highlight reconstruction Tier 1 shipped (PR
metadata: 
  node_type: memory
  type: project
  originSessionId: c033d948-2981-4ec4-ae88-f352a0850ff8
---

Raw highlight reconstruction, Phase 1 of 2 (PR #46, branch `feat/highlight-recovery-tier1`, 2026-06-02).

**Built:** `src/lrt_cinema/highlight_recovery.py` — `reconstruct_highlights(camera_rgb, asn)` + `clip_mask` + `_tier1_ratio_propagation` (returns `(out, tier2_mask)`). Camera-space pre-stage ("Stage 1.5") inserted in `pipeline.render_frame` POST-demosaic / PRE-Stage-2-WB, gated by `render_frame(highlight_recovery=False)` (default off). Tier 1 = cross-channel ratio propagation: clipped channel ← `max(cam_c, s·m_c)`, `s = Σ_surv cam / Σ_surv m`, `m_c` = box-mean over unclipped same-channel neighbours (`develop_ops._box_sum`, radius 8). Fully-blown (0 survivors) → `∝ AsShotNeutral·clip_level` (post-WB neutral, NOT camera [1,1,1] = magenta) + flagged in `tier2_mask`.

**THE load-bearing finding (expensive to re-derive):** Adobe's Stage-9 ProfileToneCurve **clamps ≥1.0→1.0** (real D750 curve verified) and runs BEFORE every LR tone op, so recovered over-white headroom is erased on every **tap-9** path — the default `lrtimelapse` sRGB AND `cinema-linear-finished`. Measured **0% sRGB change** on DSC_4053 (the gym/window frame; blown windows already render ~[240] neutral ≈ the LRT JPG ~[238] — the task's "dark+warm 0.96" premise did NOT reproduce). Benefit is realised ONLY on **tap-7 `cinema-linear-master`** (no tone curve): warm/magenta blown highlights → neutral (ProPhoto `[1.74,1.07,1.81]→[1.22,1.15,1.23]`). Hence CLI auto-default: ON for cinema-linear-master, OFF for tap-9.

**Clamp-before-develop-ops is a DEFECT for graded sequences, not "correct, not a gap"** (I retracted that framing after user pushback). Stage 9 clamps before Stage 11 `apply_exposure_2012`, so an exposure/highlights pulldown can't surface the recovered/over-range highlights — **proven: faithful recovery ON==OFF byte-identical even under a synthetic −3 EV pulldown** on DSC_4053. (Cite THIS byte-identity, NOT the tap-7 "std 24 vs 13.75" detail gap — tap-7 drops the whole tone curve, confounded.)

**VALIDATION REFRAME (user directive, 2026-06-02 — load-bearing):** `dng_validate` ΔE (gym 0.026) is DEMOTED to a **regression tripwire** for baseline colour science (stages 1-9), NOT the north-star. **North-star = matching the LRT JPG look**, then exceeding it in targeted areas (highlight/shadow recon, sharpening, NR). Do NOT let 0.026 drive decisions against the real goal — it's an active footgun ("keep highlights byte-identical to a renderer that throws highlight data away"). DECISIONS §9; CLAUDE.md north-star rewritten.

**Current sequence is CONSTANT-NEUTRAL grade:** all 5033 frame XMPs have `crs:Exposure2012=0`, Highlights/Whites/Shadows=0, constant Temp 4034; only ±0.05-stop `LocalExposure2012` deflicker. So the clamp defect is **NOT exercised by this deliverable** (windows already match the JPG), and the reorder below is a near-no-op here. The defect bites EXPOSURE-RAMPED sequences.

**Reorder PROPOSAL (DECISIONS §10, NOT decided/implemented):** apply scene-linear develop tone ops (Exposure2012, future faithful Highlights) BEFORE the DCP ProfileToneCurve; tonemap+clamp last. Identity case reproduces dng_validate (gate is stages 1-9, no develop ops → ORTHOGONAL, the "gate tension" was largely illusory). Go/no-go = render both orderings on an EXPOSURE-RAMPED frame, compare each to its LRT JPG (reduces residual→adopt; worsens→ACR-order assumption wrong). Caveat: `tone(x·2^EV)≠tone(x)·2^EV` changes ALL pixels for EV≠0; ACR's exposure-order is UNVERIFIED; this constant-grade sequence CANNOT validate it (need a Holy-Grail day↔night seq).

**Phase 2 (Poisson) inherits the tap-7-only confinement** (also pre-WB → also hits the Stage-9 clamp). Reprioritize: the reorder (unlocks the faithful default) is likely higher-leverage than Poisson (stays EXR-only) — but both need an exposure-ramped test sequence to show value on the real goal. Reconsider before investing in Phase 2.

Authority: `docs/DECISIONS.md` §8/§9/§10, `docs/PIPELINE.md` §"Stage 1.5"/§2.5, CLAUDE.md §"Validation invariants". Tests: `tests/test_highlight_recovery.py` (fixture-free).

## ORDER/DOMAIN AUDIT OUTCOME (2026-06-03) — `docs/research/pipeline-order-audit.md`

Full adversarial pipeline order/domain audit done (5 parallel sub-agents + advisor). Key EXPENSIVE-to-re-derive findings beyond the §8/§10 story above:

**§10 reorder is HARDER than DECISIONS §10 documents — THREE serial headroom clamps, not one.** §10 names only the Stage-9 ProfileToneCurve, but on the default tap-9 path headroom is destroyed by, in order: (1) **Stage 7** ExposureRamp `min(linear,1.0)` (`pipeline.py:179-180`, the BINDING clamp today); (2) **Stage 8** LookTable `clip(v_encoded·val_scale,0,1)` in its srgb_gamma branch (`lut3d_baker.py:211`); (3) **Stage 9** curve `[0,1]` (`pipeline.py:124,148`). Deferring only Stage 9 is insufficient — Stage 7 then Stage 8 still clamp. WORSE: the **LookTable cube is authored in a [0,1] sRGB-encoded V domain** (`lut3d_baker.py:150`) → it **saturates at its top cube entry** for V≥1, so preserving overrange needs **out-of-cube extrapolation**, not just clamp removal. This is a real feasibility obstacle → §10 stays a gated proposal, now with a bigger price tag. (D750 ships NO HueSatMap, so Stage-5 HSM clamp never fires; only Stage-8 LookTable matters.)

**NEW finding F2b — `cinema-linear-finished` is tap-9 AND defaults PERCEPTUAL = domain mismatch.** Code-confirmed: `STAGE_7_PRESETS={"cinema-linear-master"}` only (`presets/__init__.py:34`); `stop_after_stage = 7 if preset in STAGE_7_PRESETS else 9` (`cli.py:351`) → cinema-linear-finished = tap-9 (Stage-9 tone curve + [0,1] clamp). But it's in `_PERCEPTUAL_DEFAULT_PRESETS` (`cli.py:68`). So scene-referred perceptual ops (DR-compression's fixed scene-linear **0.18 log anchor**; "no top clamp / overrange→RGC") run on tone-curved, clamped data → anchor no longer at scene midgray, overrange-preservation moot. The perceptual scene-referred thesis only holds on **cinema-linear-master (tap-7)**. Latent (perceptual-only sliders 0 on the constant-neutral deliverable). RESOLVE = preset-default decision for owner (default it FAITHFUL, or gate scene-referred ops off tap-9). Also new: ToneCurvePV2012 is intent-INDEPENDENT + clamps [0,1] (`develop_ops.py:850,101`) → kills tap-7 overrange too if a user tone curve is set.

**Deflicker (re)confirmed exhaustively: 1:1, NO scale anywhere, NO double-apply** (`interpolation.py:93,127` add; `apply_deflicker` vs `apply_lrt_mask_offsets` read disjoint mutually-exclusive sources). 3× under-delivery = UNITS not domain (matches [[lrt-jpg-northstar-baseline]]; refuted the task's domain premise). Deliverable DOES run develop_ops (`cli.py:351-358`); the stages-1-9-only path is the ΔE *gate*, not the worker.

**SDK: local /tmp empty BUT public mirror exists.** `/private/tmp/dng_sdk` is empty (`find -name '*.cpp/.h'`=0, 2026-06-03, /tmp-ephemeral) — so re-checkout for local work. HOWEVER the meta-audit found the SDK + spec ONLINE and they ANSWER the order question: `dng_render.cpp` mirror (github aizvorski/dng_sdk, Android googlesource) + DNG 1.6 spec PDF (paulbourke). Use those for primary-source order cites, not just code.

## META-AUDIT OUTCOME (2026-06-03) — `docs/research/pipeline-worldclass-gap-and-plan.md`

Ran a ruthless adversarial meta-audit of the order/domain audit (7 parallel research+code agents, primary-sourced). Built a bifurcated world-class spec (path A = reproduce Adobe; path B = scene-referred master) + gap analysis + gated plan. Key cross-session facts:

**PRIMARY-SOURCED Adobe order (overturns the "UNVERIFIED" label):** DNG spec + `dng_render.cpp` pin the PROFILE chain **WB→HueSatMap→Exposure(ramp)→LookTable→ProfileToneCurve→encode** — spec verbatim "LookTable applied after exposure… but before any tone curve stage" → exposure IS pre-tone-curve [SPEC/SDK]. BUT the reference renderer has NO PV2012 Basic ops (Contrast/Highlights/Whites/Blacks) — ACR's actual develop-panel order is CLOSED/unverifiable. So §10 reorder stays gated by EVIDENCE. New detail: reference NEGATIVE exposure is folded INTO the tone curve (dng_1d_concatenate), positive = ramp w/ rounded shoulder — not a clean scene-linear multiply.

**What the order/domain audit MISSED (structural blind spots):** (1) entire MISSING subsystems — local/masked adjustments (SILENT: only HG/Deflicker/Global LocalExposure2012 → global EV; mask geometry discarded, user masks skipped, LocalContrast/Clarity/Dehaze/Temp/Tint never read, xmp_parser.py:309-360), lens/DNG-OpcodeList3 (vignette/distortion/CA, render reads NONE — SILENT), NR (absent), **sharpening = SILENT no-op** (develop_ops.py:766, parsed+threaded, NOT in _warn_dropped_ops), crop/geometry, Dehaze. _warn_dropped_ops covers ONLY H/S/W/Texture/Clarity → honesty-invariant breach. (2) **Demosaic = bilinear** (DemosaicAlgorithm.LINEAR, ≥5.5dB below SOTA per Malvar; chosen to match the RETIRED dng_validate target) = a hidden source of the north-star EDGE residual the audit blamed only on sharpening. (3) **Highlight recovery runs POST-demosaic** — SOTA (darktable) = on the MOSAIC before bilinear smears clip boundaries; the code's own ~1% threshold-drop comment is self-evidence. (4) **Temporal coherence** (it's a TIMELAPSE tool) — #1 pixel-domain gap = motion-compensated temporal NR (master has no safety net; TIFF backstopped by LRT re-deflicker+MotionBlur); + chromatic deflicker; validation metric = E_warp (optical-flow). (5) Verified perceptual ops at OP level but missed MASTER level — BUT these path-B critiques are sub-agent RESEARCH OPINION needing verification, NOT code-verified (advisor caught me over-promoting them; only F2b is code-verified). Corrected: **RGC terminal placement is NON-CANONICAL, not a defect** — RGC's recommended placement is input-side, but its compress-toward-achromatic math is provenance-agnostic and PIPELINE.md §7 chose the terminal placement DELIBERATELY (gated on actual out-of-AP1). Real open question = does ACES 2.0's INTEGRATED DRT gamut-mapping make a terminal RGC redundant/double-compressing? VERIFY (graded master → ACES 2.0 round-trip) before changing; do NOT "remove as off-spec" (that was the over-claim). Master arguably should be ACES2065-1 not ACEScg (opinion). The minimalist "ship a CLEAN master, no baked creative ops" argument CHALLENGES the v0.9 perceptual investment BUT directly contradicts the deliberate §7 demo-purpose — it's a FORK (D1), not a verdict. ACES 2.0 SHIPPED (OCIO 2.5, Resolve 20).

**SELF-CORRECTIONS:** F8 OVER-flagged (inert on production profile — see SDK line above). The "display-referred-in-the-middle = disease" instinct is WRONG for path A (correct-by-design, mirrors Adobe, 0.026 proves it) — generic-scene-referred ideal mis-condemns the faithful path (the method's main trap). F3 deflicker units fully reconfirmed.

**OWNER DECISIONS the analysis can't resolve:** D1 master identity (clean ACES2065-1 minimal vs better-primitives demo — drives whether to strip perceptual ops); D2 sharpening policy per-path (bake on sRGB-TIFF to match JPG vs none on EXR); D3 local-mask scope. Plan is gated (nothing run locally: no SDK, no fixtures).
