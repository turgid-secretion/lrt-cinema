- [develop-op-expansion-playbook](develop-op-expansion-playbook.md) — recipe for baking a dropped LR develop op into Stage 12 (sub-dataclass + thread blend/merge/meaningful + byte-exact identity + Axis-1 oracle); next is Texture/Clarity
- [ship-gate-render-path](ship-gate-render-path.md) — gym/rose ΔE gate renders stages 1-9 only (no develop_ops); Stage 11-12 changes are orthogonal to it. CORRECTION (2026-06-05): the system **Nikon D750 Camera Standard.dcp** DOES exist locally (`/Library/Application Support/Adobe/CameraRaw/CameraProfiles/Camera/Nikon D750/`) — `render_frame(DSC_4053.dng, …)` runs a REAL end-to-end render here, so the gym path is NOT DCP-blocked (see capture-sharpening-d2)
- [grading-fidelity-and-frontier](grading-fidelity-and-frontier.md) — no open tool checks Adobe develop-op fidelity (only ACR); the grading-sweep harness (Tier 0 CI / Tier 1 ACR-fit); dual-mode --render-intent is decided + DR-compression (Highlights/Shadows/Whites) SHIPPED on perceptual (PR #34); OKLCh/CDL still alias faithful; RGC/local-Laplacian/Texture are follow-ups
- [render-perf-backend](render-perf-backend.md) — optional numba backend (lrt_cinema.accel) + proxy preview (PR #41); accelerates DCP-render stages 1-9 + encode (6.6× / 7.1× pool, colour-identical to numpy); Stage-12 faithful grade ops NOT yet accelerated (graded frame ~1.8×) — the #1 follow-up
- [highlight-recovery-tier1-and-stage9-clamp](highlight-recovery-tier1-and-stage9-clamp.md) — raw highlight reconstruction Tier 1 shipped as pre-WB Stage 1.5 (PR #46); the Stage-9 tone-curve clamp confines Tier 1 AND the upcoming Tier-2 Poisson to the tap-7 EXR path, not the default sRGB — read before Phase 2; also holds the VALIDATION REFRAME (dng_validate demoted to regression tripwire, LRT-JPG = north-star, no footgun) + the ORDER/DOMAIN AUDIT outcome (docs/research/pipeline-order-audit.md): §10 reorder = THREE clamps not one (Stage 7 ramp / 8 LookTable [0,1]-cube / 9 curve) so it's harder than documented; F2b cinema-linear-finished is tap9 yet PERCEPTUAL-default (domain mismatch). PLUS the META-AUDIT (docs/research/pipeline-worldclass-gap-and-plan.md): order/domain audit was blind to MISSING SUBSYSTEMS (local masks/lens-opcodes/NR/sharpening all SILENT drops), demosaic=bilinear (hidden edge-residual source), HL-recovery is post-demosaic not on-mosaic, temporal-NR gap, RGC terminal placement non-canonical-but-DELIBERATE (verify vs ACES2.0, NOT a proven defect — advisor caught me over-promoting sub-agent opinion); Adobe PROFILE order now PRIMARY-sourced (dng_render.cpp mirror + DNG spec online — exposure pre-tone-curve, but PV2012 develop panel still closed); F8 was over-flagged; owner forks D1 master-identity / D2 sharpening / D3 local-masks
- [trunk-branch-overhaul](trunk-branch-overhaul.md) — the trunk/branch pipeline overhaul (branch feat/trunk-branch-overhaul, plan docs/research/pipeline-overhaul-plan.md): THE MODEL (2 products opposite definitions of correct; per-frame intent MUST bake = no NLE transport; temporally-varying-vs-constant axis); owner decisions (per-frame bakes + --master-look default defer; DCB-now/RCD-later; flag-gated owner-validates); local validation can't run gym/rose/LRT-JPG/demosaic-edges; **Phase A SHIPPED** (commit f65d674: A1 sharpness-warn, A2 F2b fix, A3 --master-look, A4 --demosaic, byte-exact-by-default, 250 tests green); Phase B/C pending
- [lrt-jpg-northstar-baseline](lrt-jpg-northstar-baseline.md) — first LRT-JPG north-star baseline (DSC_4053↔LRT_00001): median ΔE 1.71, color matches; the gap is a PV2012 tone-curve-SHAPE difference (LRT = darker-highlight shoulder + lifted-shadow toe vs our DCP baseline), NOT color/bug/BaselineExposure/highlights — confirms ours≈dng_validate≠LRT; lever = emulate PV2012 tone (diverge from dng_validate)
- [deflicker-rootcause-audit](deflicker-rootcause-audit.md) — B2 AUDIT (commits 4de7a7a + correction): the deflicker "~3× under-application" is REFUTED. The defect is a SCALAR-GAIN CONFLATION (a no-offset gain `LRT≈g·ours` mixes exposure with tone-shape → a fixed tone-curve diff back-solves into a fake deflicker factor) — NOT a gamma artifact (the gain is ALREADY linear; linear≈gamma gain to ~1% — an earlier draft's "gamma-domain" claim was wrong + corrected). Keep `--deflicker-scale 1.0` on 3 legs: scalar-conflation, short-term-BY-DESIGN (so the long-term drift isn't deflicker's job), + a linear preview test (scaling ≥2× worsens flicker). GENERAL LESSON: don't summarise an LRT-vs-ours comparison with a scalar gain when the difference is a CURVE. Real #1 lever = PV2012 tone (§11). Real production sequence + LRT JPGs + previews on the SanDisk drive (renders runnable locally; render_frame works NEF-direct via rawpy)
- [capture-sharpening-d2](capture-sharpening-d2.md) — D2 SHIPPED (PR #47, commits 81dd632+fc66bb1): apply_sharpness un-stubbed → clean-room ACR capture USM, all 4 knobs (Amount/Radius/Detail/Masking), FAITHFUL-only, `--capture-sharpen {off,xmp,acr}` default off=byte-exact; sRGB-OETF luminance domain (headroom-preserving), §5 amendment cites §9/§11; Axis-1 oracle <1e-6 + END-TO-END smoke on real DSC_4053 (acr +50% / xmp +126% HF energy); HONEST STATUS = capability shipped, the 4 tuning constants are UNVALIDATED placeholders (owner sweeps vs LRT JPG); numba-accel = follow-up
- [vertical-cyan-rootcause](vertical-cyan-rootcause.md) — the SATURATED VERTICAL CYAN at window-blind edges (owner-flagged, real defect, NOT in LRT, NOT intent) root-caused + fix tested. ROOT = DEMOSAIC directional color error at steep edges (rcd 8× linear) AMPLIFIED by cool develop WB (4034K); clip is a MARKER (steepest edges, 6.2× coincidence) NOT the root (42% of cyan isn't near clips; on-mosaic highlight RECONSTRUCTION tested + REFUTED, 3 variants, 2 worsened). EXONERATED: stage-12 grade, sharpening, gamut/perceptual, highlight-recovery, demosaic-CHOICE-at-as-shot-WB. PARTIAL FIX (env-gated, default off=byte-exact): pre-WB chroma-difference median (`LRT_CINEMA_CHROMA_MED`, ~40% cut, HF/detail preserved) + linear demosaic (halves rcd) → best 0.006 vs LRT 0.003 (close, not full). SAME class as the horizontal-slat aliasing. Metric lesson: artifact = high Lab C\* + local hue-standout, NOT red-deficiency; scalar metrics misled me twice + I wrongly said "visually conclusive" — LOOK + trust the colorist. Advisor twice stopped me from the tidy narrative (intent; then clip-reconstruction)
- [blinds-false-color-survey](blinds-false-color-survey.md) — primary-source survey + dng_validate CAPSTONE on the venetian-blinds demosaic false color (docs/research/demosaic-false-color-literature-survey.md). FACT: it's a fully-characterized FUNDAMENTAL failure mode (luma-chroma freq-multiplexing; near-Nyquist axis-grating lands on the C2 chroma carrier); our ~0.56 = the demosaic floor (even real AMaZE). Universal software fix = post-demosaic chroma-difference median, but OSS ships it OFF-by-default + generic (smears real chroma); NO method (incl Adobe's) demonstrated on the dense-periodic case. ACR: NO patent; the one documented Adobe mechanism (DNG default chroma blur) MEASURED 0.70 via dng_validate on real DSC_4053 → FALSIFIES it as ACR's mechanism; ACR's 0.28 is undocumented+unmeasured (make NO claim). D750 = WEAK OLPF (capture-side contributor). A fix = LOSSY detect-then-localize chroma suppressor, NOT a clean solve. Reusable LOCAL oracle: NEF→Adobe DNG Converter→dng_validate→metric. Corrected my prior "adaptive edge-aware reconstruction / demosaic contributes nothing" overreach
- [demosaic-test-fixtures](demosaic-test-fixtures.md) — demosaic fixture research: verdict (b) no external set verifies world-class raw-linear demosaic (all public sets = circular sRGB); plan = own layered battery (docs/research/demosaic-test-fixtures.md); RCD clean-room ALREADY EXISTS (_rcd_demosaic.py, PSNR-only beats-bilinear bar = unfalsifiable); THE TRAP (matching AMaZE/RCD = port-fidelity≠world-class); DCB=BSD-3/Gozdz not LGPL (verified+fixed); D750 SSF unpublished→D5100 sub justified
- [alt-raw-engine-feasibility](alt-raw-engine-feasibility.md) — DON'T swap render path to RawTherapee/darktable/RapidRAW: the "RT is clean" revelation is a WB CONFOUND (RT rendered warm 5713 as-shot, not LRT's cool 4034; cyan only blooms at cool WB, even with AMaZE-tier menon — switching doesn't fix it). 2 independent kills: no engine reads Adobe crs: intent (darktable proven 0/24.3M px differ) + swap forfeits LRT deflicker (lives in the XMP). RapidRAW=no CLI(DQ), darktable=binary-blob sidecars, RT=only viable shape but look≠ACR + macOS CLI sandbox-broken. Real lever = tune LRT_CINEMA_CHROMA_MED (flag-gated, ~40% partial)
