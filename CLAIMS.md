# CLAIMS.md — the authoritative claim ledger

Owner-approved: 2026-06-10

**This file outranks every other prose surface in this repo** (docs/, memory,
commit messages, docstrings). Those surfaces accumulated confident falsehoods
during the May–June 2026 LLM sprint; the full audit is in the repair plan
(owner's copy) and the failure post-mortem will live in `docs/archive/`.

Rules: a claim enters as VERIFIED only with a regeneration command + artifact;
numbers expire to STALE after 30 days unless re-run; REFUTED claims stay listed
(with their refuting evidence) so they cannot be re-derived from old prose.

Statuses: **VERIFIED(date)** · **UNVERIFIED** (plausible, not yet re-run) ·
**HYPOTHESIS** (proposed explanation, experiment pending) · **BLOCKED(needs X)**
· **REFUTED(evidence)**.

## Render correctness (stages 1–9 vs Adobe ground truth)

| Claim | Status | Regenerate / evidence |
|---|---|---|
| Gym end-to-end mean ΔE2000 = **0.0230** vs dng_validate (P50 0.000, P95 0.193, max 13.58, 99.90% px <1.0) — re-pinned after the WB-before-demosaic fix (was 0.0262/0.320/2.06) | **VERIFIED 2026-06-10** | `python3 -m pytest tests/test_pipeline.py::test_ship_gate_gym_de_under_1` against `~/lrt-cinema-fixtures/DSC_4053_dngvalidate.tif` (recipe in FIXTURES.md). Mean + P95 IMPROVED; the max regression is localised to **1448 px = 0.006%**, all at ≥97%-white CFA neighbourhoods (verified by raw-domain census): partially-clipped highlights where dng_validate reconstructs and the canonical scale-then-clip lands clip-to-white with a per-channel cap. SUSPECT row queued for REFERENCE_PIPELINE.md "highlight handling"; owner-eyeball on production blown windows pending (task D) |
| Gym gate (pre-H1-fix lineage) = 0.0262 (P95 0.320, max 2.06) | **SUPERSEDED 2026-06-10 (kept for history)** | Measured with demosaic-before-WB (the H1 ordering bug) — the bug the gate could not see (bilinear commutes on unclipped px; rule 8). Gate had been **dormant** (fixture evaporated from /tmp); resurrected same day |
| Synthetic flat-patch gate: neutral median ~0, chromatic mean ~0.05 | **VERIFIED 2026-06-10** | `python3 -m pytest tests/test_synthetic_dng.py` (2 passed, 35s) |
| Rose mean ΔE = 0.545 | **BLOCKED(needs rose.dng)** | Fixture missing from this machine; restore `rose.dng` + regen `rose_dngval_Camera_Standard.tif` per FIXTURES.md, or strike the number |
| Full pipeline byte-exact at zero sliders | **UNVERIFIED for stages 1–9** | Stage-12 ops have explicit byte-exact identity tests; stages 1–9 zero-op identity has NO direct test → Phase-1a adds one |
| Test suite: **577 passed, 3 skipped**, ruff clean (post-purge) | **VERIFIED 2026-06-10** | `python3 -m pytest -q && python3 -m ruff check .` (126s). Delta vs the 573/4 pre-purge baseline is fully explained: +3 context-budget guard tests, +1 gym end-to-end gate flipped SKIP→PASS (fixture resurrected). Render behavior unchanged (gym ΔE identical 0.0262). Historical "250 tests green" and "485 tests" both wrong |
| Suite skip count is SanDisk-mount-dependent: **578/2 mounted, 577/3 unmounted** (same 580 collected) | **VERIFIED 2026-06-10** | `tests/fixtures/raw/sample.NEF` is a symlink onto the drive; `test_real_dnglab_smoke` skips when it dangles. Verified by running the suite pre-/post-change at the same HEAD with the drive mounted (578/2 both times) |
| Phase-0/0.5 purge was behavior-neutral | **VERIFIED 2026-06-10** | Gym gate 0.0262 before and after; suite green; changes were prose/fixture-path/guard-test only |
| WIP env-gates (`LRT_CINEMA_B1`, `LRT_CINEMA_CHROMA_MED`) are inert by default | **MOOT — gates deleted 2026-06-10 (owner-approved)** | Were verified inert (`9daed8f`); removed with the B1 module after the H1 fix shipped |

## The look gap (vs LRTimelapse / Lightroom output)

| Claim | Status | Regenerate / evidence |
|---|---|---|
| **North-star JPGs were rendered by LRTimelapse Pro 7.5.3's INTERNAL engine — NOT Lightroom/ACR** | **VERIFIED 2026-06-10** | EXIF `Software` tag on `LRT_00001.jpg` (now in `~/lrt-cinema-fixtures/production/lrt-jpg/`). The entire first-sprint "ours≈dng_validate≠ACR mystery" framing had the wrong renderer on the reference side. OPEN OWNER DECISION: target look = LRT-internal (what was approved) or Adobe-canonical? |
| Production XMPs (DSC_4053–4302): **ALL global develop sliders are ZERO** | **VERIFIED 2026-06-10** | Raw-XML census (NOT via our parser): `tests/fixtures/evidence/xmp_census_2026-06-10.json`. H/S/W=0, Exposure/Contrast/Blacks=0, HSL/ColorGrade=0, ToneCurve='Linear'. Active intent: WB 4034K/+20 constant, Sharpness 25, ColorNR 25, deflicker EV ±0.048/+0.076. Colorist masks "LRT Mask 1–5" carry zero adjustments |
| "The look gap is the dropped Highlights/Shadows/Whites sliders" (this repair's own leading hypothesis) | **REFUTED 2026-06-10** | The census: those sliders are zero in production. Pre-registration caught our own tidy narrative |
| Look-gap re-attribution: LRT-internal engine's BASELINE rendering (its own base tone/color treatment + Sharpness 25 + CNR 25 + 8-bit JPEG) differs from the Adobe-canonical baseline we implement | **HYPOTHESIS (now leading)** | Decisive test: fresh LR Classic export of the same XMPs should land near OUR render (we match Adobe at 0.026), both differing from the LRT JPG by the same signature. With all sliders zero + constant WB, the residual should be ONE fixed global transform — fittable from 250 frame pairs, no closed-source reverse-engineering needed |
| Exact ACR match is achievable | **REFUTED(by construction)** | Adobe PV2012 Highlights/Shadows are local-adaptive (Local-Laplacian class, per Adobe docs) — moot for this sequence anyway (sliders zero) |
| B2 audit: "deflicker scale 1.0 confirmed correct; the '~3×' was a scalar-gain conflation artifact" | **REFUTED 2026-06-10** | Three independent measurements: (1) per-frame gain vs deflicker EV across 250 frames: **r = +1.000**, slope +1.66 output-domain (vs r=0.75 for scene brightness — kills the "it's the tone curve" alternative); (2) three-point arms on frame 250 (EV +0.072): off→gain 1.124, 1.0×→1.081, **3.1×→1.006 (flat)**, ΔE 2.73/2.30/**1.49**; (3) full-sequence 3.1× confirmation: **gain flat 1.005→1.007 across all 250 frames (was 0.941→1.082), sequence mean ΔE 1.20→0.89, worst frame 2.45→1.58, zero color cast**. The dead sprint's original "~3× under-application" was approximately RIGHT; the audit's conflation critique was methodologically fair but killed a true signal. Evidence: `tests/fixtures/evidence/seq_lrt_compare_scale{1.0,3.1}_2026-06-10.json` |
| **Lightroom ITSELF applies ≈3–4× the serialized `LocalExposure2012` — our mask-exposure emulation has a REAL BUG (not a look choice)** | **VERIFIED 2026-06-10 (LR-arbiter test, owner exports)** | Fresh LR Classic 14.5.1 16-bit exports of DSC_4053 (EV −0.048) + DSC_4302 (EV +0.072): LR lands on our 3.1×-arm at BOTH frames incl. the sign flip (gain vs our 1×: 0.937 / 1.082; vs our 3.1×: 1.004 / 1.008). ΔE keeps improving to 4.0–4.5× (clip-biased LSQ gain understates; perceptual optimum ≥4.0). Mechanism confirmed by the calibration row below |
| **Mask-exposure fix SHIPPED**: `apply_deflicker`/`apply_lrt_mask_offsets` write `LR_LOCAL_EXPOSURE_SCALE`(=4.0)`×serialized` onto the new scene-referred `DevelopOps.scene_exposure_ev`; `render_frame` (and the MLX renderer) multiply camera RGB by `2^EV` pre-Stage-2. `--deflicker-scale` = owner trim on the corrected baseline (1.0 = LR-faithful). HG/Global kinds get the same ×4 (same serialization; mechanism-derived — they are zero in this production sequence) | **VERIFIED 2026-06-10** | `python3 -m pytest tests/test_interpolation.py -q` (semantics) + suite green 578/2. Sequence-level validation: task-D seq re-run below. History: order-audit F3 (2026-06-04) flagged ~3×; B2 wrongly refuted it; LR-arbiter + CAL re-confirmed |
| **Exact mask-exposure factor = 4.0, linear, applied SCENE-REFERRED (pre-tone-curve)** — XMP `LocalExposure2012` stores EV/4; LR applies `2^(4·serialized)` as a linear gain upstream of the tone curve. Pre-registered prediction ("4.0 exactly, linear") CONFIRMED | **VERIFIED 2026-06-10 (CAL experiment, owner exports)** | `python3 tools/cal_deflicker_factor.py` → `tests/fixtures/evidence/cal_deflicker_factor_2026-06-10.json`. Single-variable CAL XMPs (serialized 0.25/0.50, xmp-diff-verified) vs LR Classic 14.5.1 16-bit exports. Scene-referred k-scan: k\* = 4.002/4.004 (ΔE-mid parabola), 3.984/3.977 (midtone gain-unity); mean 3.992, spread 0.027; ΔE at k=4.0 = **0.204/0.435** (at/below the base-look floor). Critical negative result: the production-path scan (post-ProfileToneCurve `exposure_ev` multiply) has NO interior minimum ≤5.25 and ΔE_mid ≥4.0 even at matched midtone gain — **the application domain must be scene-referred**; a bare ×4 in the post-curve layer cannot reproduce LR at ±1–2 EV (HG-ramp magnitudes). First run of the scan hit a `Path.with_suffix` filename collision (k=3.5 silently reused the k=3.0 render); fixed in-script, renders regenerated |
| LRT-internal engine ≈ Lightroom/ACR on this sequence — the "which look" dilemma dissolves | **VERIFIED 2026-06-10** | LRT-JPG vs fresh LR TIFF: gain 1.002/0.999, ΔE 0.585/0.508 on the two test frames — within ~the 8-bit JPEG floor + sharpening/CNR differences. The owner-approved look IS (near enough) the Adobe look; our 16-bit chain at corrected deflicker sits 0.54–0.97 ΔE from LR |
| North-star sequence gap (mean ΔE 1.20, drift 0.94→1.08, worst 2.4 at sequence end) is dominated by the deflicker channel; residual base-look floor ≈0.7 ΔE at matched brightness | **VERIFIED 2026-06-10** | seq compare at scale 1.0: ΔE minimum 0.68 exactly where gain crosses 1.0 (frame ~125). Remaining floor = LRT-internal base look + Sharpness 25 + CNR 25 + JPEG, at 6× downsample |
| **Post-fix sequence verdict: mean ΔE 0.62 vs the approved LRT JPGs (range 0.58–0.66), drift ELIMINATED** — trend 0.63→0.64 flat, gain 0.995→0.992 (never crosses 1.0), R−B gain Δ 0.003 (no cast), worst frame 0.66 (was 2.45). At the measured base-look floor (LRT-jpg vs fresh-LR ≈0.51–0.59 + sharpen/CNR/JPEG) — the deflicker channel's contribution is gone | **VERIFIED 2026-06-10** | 250-frame re-render at DEFAULTS (corrected deflicker + WB-before-demosaic), `python3 -m lrt_cinema render --input <drive NEF dir> --output /tmp/seq_current --target lrtimelapse --workers 8 --dcp <D750 Camera Standard>` then `python3 tools/seq_lrt_compare.py` → `tests/fixtures/evidence/seq_lrt_compare_fixed_2026-06-10.json`. vs OLD pipeline: 3.87 mean (expected — the fixes change production output by design). Owner-eyeball grid: `tools/owner_ab_grid.py` → `~/lrt-cinema-fixtures/verify-2026-06-10/owner_ab_grid.png` (pending owner verdict) |
| Current-vs-old-export render difference: mean ΔE 3.66, **constant chromatic** (gain R 0.94 / G 1.01 / B 0.87, stable f1→f250) | **MEASURED 2026-06-10, UNEXPLAINED** | Old drive TIFFs carry the same stale version string ("0.7.1a0") — undatable, flags unknown. Exposure-class ruled out (G≈1); it's a WB/color-class difference. Root-cause alongside H1–H4 (same "what changed when" family). Process lesson: the never-bumped version number makes artifacts undatable |
| LRT ingests our `LRT_*.tif` 16-bit sequence (round-trip premise) | **VERIFIED 2026-06-10 (owner-run)** | Owner rendered the 250-frame lrt-cinema TIFF sequence through LRTimelapse Pro 7.5.3 → `tif_H265-444_Rec.709L_OriRes_59.94_UHQ.mov` on the drive. The product's existential premise is demonstrated (TIFFs were a pre-repair build; re-confirm after any emission change) |
| Owner's RT refuting experiment parameters | **VERIFIED 2026-06-10** | `~/lrt-cinema-fixtures/production/rt-experiment/DSC_4053.NEF.pp3`: Temperature=4039 Green=1.057, demosaic Method=rcdvng4 — RT at the cool develop WB with RCD-class demosaic, no cyan. Anchor observation for H1–H4 |
| Frame mapping LRT_00001 ↔ DSC_4053 | **VERIFIED 2026-06-10** | Identical EXIF DateTimeOriginal (2026:05:20 08:46:53) on both |

## The cyan/blinds artifact

| Claim | Status | Regenerate / evidence |
|---|---|---|
| "Fundamental demosaic false-color floor, amplified by cool WB; engine swap wouldn't help" | **REFUTED(owner experiment, 2026-06-10)** | Owner ran RawTherapee at the cool develop WB, multiple algorithms incl. bilinear + RCD, same raw: **no artifacts**. The repo's claim was extrapolated from OUR menon, never tested in RT (see header on `docs/archive/research/alt-raw-engine-feasibility.md`) |
| **H1 CONFIRMED: the cyan root cause is demosaicing the UN-white-balanced CFA** (we pass unit WB to libraw and feed raw mosaic to rcd/menon; Stage 2 scales after — RT/LR scale first) | **VERIFIED 2026-06-10** | Single-variable A/B (`tools/h1_wb_demosaic_ab.py`, develop WB 4034K/+20, artifact region auto-located by cyanness(ours)−cyanness(LR)): saturated-cyan tail (P99.5×1000) rcd 187.7→**87.2**, menon 197.8→**94.5**, vs **LR-Classic 84.5** — WB-before-demosaic lands us ON Adobe's level (mean 10.98 vs LR 10.93). Evidence: `tests/fixtures/evidence/h1/` |
| H2: our clean-room demosaic ports are buggy | **REFUTED as cause** | The independent BSD menon shows the identical artifact and identical H1 response — input conditioning, not implementations |
| H3: post-demosaic stage amplifies / H4: input-path difference | **MOOT** | Artifact forms at demosaic (H1); cool WB amplifies as understood, downstream stages and input path are not the cause |
| "Fundamental demosaic false-colour floor — all engines have it; switching wouldn't help" (dead-sprint narrative) | **DEAD** | The floor exists, but our artifact sat 2.2× above it due to the H1 ordering bug; correctly conditioned we sit at ACR's floor. Owner's RT instinct was right |
| Architecture gate input: bespoke raw front-end viability | **BESPOKE SURVIVES, STRENGTHENED** | Root cause = discrete fixable ordering defect, not a diffuse quality gap. Phase-3 fix: pre-scale CFA by WB at demosaic (all paths incl. libraw user_wb), re-pin gates after |
| **OWNER EYEBALL VERDICT (rank-1 ground truth): the obvious colour artifacting is GONE in the fixed renders** (native-res flip-stack, frames 1/125/250) | **VERIFIED 2026-06-10 (owner eyes)** | `~/lrt-cinema-fixtures/verify-2026-06-10/flip/`. Owner caveat: the old-pipeline arm ALSO shows no artifact (it was rendered WB-unaltered — corroborates the "old-vs-new constant chromatic difference" row) so it is NOT a valid before/after baseline → superseded by the single-variable pair below |
| **Single-variable before/after at native res: pre-fix conditioning P99.5 cyanness 205.2 vs fixed 103.9** (menon, full production intent, only the CFA conditioning toggled) | **VERIFIED 2026-06-10** | `python3 tools/demosaic_ab_flip.py` → `flip/LRT_00001_{E-menon-prefixbug,F-menon-fixed}.png` (4016×6016, zero scaling). The "before" arm must use a DIRECTIONAL demosaic — bilinear barely manifests the bug, which is why the old bilinear renders look cyan-clean and why the Adobe gate missed it |
| **Sawtooth / colour aliasing in our renders (old AND new) vs Adobe/LRT** (owner-observed, native-res review) | **EXPLAINED — bilinear-default signature; remedy GATED on architecture lock** | The flip-stack A-arms used the production default `--demosaic linear` (libraw bilinear, self-documented LOW quality); zipper/sawtooth aliasing is its known behaviour vs ACR's directional demosaic. The H1 fix is exactly what unblocks the quality demosaics (menon/rcd) — pre-fix they traded aliasing for saturated cyan. Default flip = a post-lock decision; eval renders: the F-menon-fixed flip arm. **Owner eval verdict 2026-06-10: "menon looks much better than bilinear for the most part"** — the post-lock default decision has its rank-1 input |
| **Owner verdict on the fixed renders (2nd round): "significantly better"; residual fringes at highlight-to-clip boundaries "look like they could be genuine CA … rather than rendering bugs"** | **RESOLVED 2026-06-10 — NOT lens CA; TWO rendering mechanisms, causally separated** (row below) | Constraints that framed it: (1) production XMPs carry ZERO CA correction (census) → LR's clean render is not CA-corrected; (2) Tier-1 recovery ~no-op there (G vs F: 199 px); (3) RT does raw CA pre-demosaic. Owner then confirmed real D-vs-F divergence ("highly saturated pixels"), asked for a deterministic test |
| **Owner correction on the c0 clip-neutralize arm: it only DE-SATURATES the artifacts — the structure remains** ("almost none of them look correct compared to lr-classic; menon-clipneutral is the closest, but it actually still has the artifacts, simply less saturated") | **VERIFIED 2026-06-10 (owner eyes; metric lesson recorded)** | The mean-fringe-saturation metric was blind to structure — a desaturated-but-structured artifact scored "≈D". Mechanism A's CONFIRMATION stands (the chroma collapse is the fingerprint) but neutralization is NOT the remedy — reconstruction is (Adobe's behaviour). All pressure metrics now split ΔL (structure) from ΔC (colour). Also: artifact folders must be self-explaining (README + arm naming) — owner couldn't tell which render was which |
| **Fringe forensics verdict: (A) clip-adjacent hue error + (B) directional-demosaic false colour — lens CA REFUTED for the measured clusters** | **VERIFIED 2026-06-10** | `python3 tools/fringe_forensics.py` → `tests/fixtures/evidence/fringe_forensics_2026-06-10.json` + 1:1 crops in `~/lrt-cinema-fixtures/verify-2026-06-10/fringe/`. Localize: 3848 fringe px (D-vs-F registered sat-delta >0.15). Raw census: 50.9% of fringe px within 4 CFA sites of a clipped site vs 1.0% control (50×). Causal arms (mean fringe-sat; D reference 0.167/0.308): **cluster 0** menon 0.408 → clip-neutralize (post-WB-neutral, 2-dilated MOSAIC clip mask) **0.179 ≈ D** — mechanism A confirmed; **clusters 1/3** clip-neutralize inert (0.498→0.498), 3×3 chroma-median ×2 inert (0.546), rcd ≈ menon, **bilinear 0.231 — cleaner than D** → the saturation is CREATED by directional interpolation (true lens CA would survive bilinear). Remedies (both post-architecture-lock): A → mosaic-mask-driven clip neutralization/reconstruction (Tier-1's post-demosaic 0.99 threshold structurally misses interpolation-smeared partial clips — the mask must come from the mosaic); B → false-colour suppression stage (canon: dcraw `-m`, libraw FBDD, RT FC-steps, ACR-internal; our 3×3 chroma-median probe was insufficient — needs the canon's actual schemes). Method note: the first clip-neutralize arm neutralized in UNBALANCED camera space (sat=0 pre-WB → WB re-colours it) and WORSENED the fringe — the same domain-error class under test, caught and corrected in-tool |
| RawTherapee order: dark/flat → black level + **camera-WB scaling** (`scaleColors`: `scale_mul = (pre_mul/maxpremul)·65535/(white−black)`, WB INCLUDED) → bad pixels → **raw CA PRE-demosaic** → demosaic (camera-balanced input) → develop-WB **delta** (`new_scale_mul/scale_mul`) + highlight recovery POST-demosaic in `getImage` → false-colour suppression (`ccSteps`) | **VERIFIED 2026-06-11 (local source read; multiplier nuance RESOLVED)** | `~/src-reading/rawtherapee/rtengine/rawimagesource.cc` (calculate_scale_mul :628, scaleColors :2761, getImage :758, FC :2982). RT SPLITS WB across the demosaic (camera pre / develop-delta post); dt+dcraw apply full WB pre — we follow dt/dcraw (ledger slot 3). Canon consequence: highlight-recon placement splits (dt pre; dcraw/RT post; Adobe in-render) |
| **Local source pass COMPLETE (RT + LibRaw + darktable @ ~/src-reading)**: dt highlights modes extracted (default = `opposed`, pre-demosaic, magic 0.995; algorithm = 3×3 cube-root opposed means + global linear chrominance offset from near-clip annulus + max(in, ref+chroma)); **RT "Inpaint opposed" = dt's opposed vendored verbatim, run POST-demosaic** (one algorithm, two engine placements — the 5b experiment isolates placement); FC-suppression canon converges on post-demosaic 3×3 chroma-difference median (dt c−G 1–5 passes / dcraw R−G,B−G med_passes / RT YIQ I-Q + box; FBDD = pre-demosaic outlier); dcraw recover_highlights (-H 3+) = coarse ratio-map growth, no engine's default | **VERIFIED 2026-06-11** | Read-to-learn only (rule 6, no vendoring). Citations + algorithm detail: `docs/REFERENCE_PIPELINE.md` ledger slots 3/5b/6 + RT/dcraw flowcharts. Clones are shallow @ master — re-clone to re-verify |
| Global-`Exposure2012` domain probe files prepared (single-variable: Exposure2012 = 1.0 / 2.0, all else identical incl. deflicker mask EV) | **READY — needs owner exports** | `~/lrt-cinema-fixtures/production/calibration/CALEXP{100,200}_4053.{NEF,xmp}` (xmp-diff-verified single-variable). Owner: import both NEFs (XMPs auto-load), export 16-bit sRGB TIFF, no resize, output sharpening OFF — then the cal_deflicker_factor harness pattern answers the domain question |
| **WB-before-demosaic fix SHIPPED** (all paths): `_cfa_demosaic` (rcd/mlri/menon) scales the mosaic per-site by the Stage-2 G-normalised multipliers and divides back after; the libraw path passes 4-value RGBG `user_wb` and divides back by `wb/min(wb)` (libraw's min-normalisation verified empirically: [2,1,1.5,1] → output ×[2,1,1.5] exactly). Render WB (incl. HG kelvin override + tint) resolved BEFORE decode so pre-scale == Stage 2. Unbalanced-camera-RGB return contract + CFA-path >1 headroom preserved | **VERIFIED 2026-06-10** | Suite 578/2 green + gym gate re-pinned above; production-frame artifact measurement = task-D h1 re-run below. Side effects: (1) libraw-path blown pixels now land NEUTRAL after Stage 2 (canonical dcraw/Adobe clip-to-white; was WB-tinted), partial clips get a per-channel cap (the gym max-ΔE row); (2) MLX entry previously DROPPED the develop tint (+20 in production!) — fixed in the same change (`accel/__init__.py`) |
| Chroma-median mitigation (~40% reduction) + B1 module | **DELETED 2026-06-10 (owner-approved)** | Built for the misdiagnosis; H1 fix shipped + verified, owner approved deletion same day. `_b1_highlight.py` + `LRT_CINEMA_B1`/`LRT_CINEMA_CHROMA_MED` gates removed; history in git |

| **Slot-3 WB-once migration SHIPPED (owner-accepted plan)**: divide-back shim + Stage-2 multiply DELETED — the decode emits BALANCED camera RGB (CFA paths: scaled-mosaic demosaic directly; libraw path: ×`wb_mul.min()` scalar rescale); `apply_adobe_pipeline` consumes balanced input (no-FM branch folds `diag(asn/asn_G)` into its matrix); MLX twin drops `diag(wb)`; `highlight_recovery` rewritten balanced-space (neutral=[1,1,1]) + driven by the new `pipeline._mosaic_clip_mask` (sensor-truth per-channel, 2-dilated — the fringe-forensics mask); **global `Exposure2012` folded into the scene-referred gain** (slot-7 verdict; Stage-11 = Blacks only; MLX twin matched) | **VERIFIED 2026-06-11 — migration EXACT, all four re-pins clean** | (1) gym gate IDENTICAL: mean 0.0230 / P50 0.000 / P95 0.1931 / max 13.578 / 99.90 %<1 (`pytest tests/test_pipeline.py::test_ship_gate_gym_de_under_1`); (2) suite 576 passed/2 skipped + ruff clean (was 578/2; −4 deleted post-curve-exposure tests, +2 new balanced/mask tests — fully explained); (3) pressure suite IDENTICAL TO 3 DECIMALS on every metric/arm/article vs the pre-migration baseline (`pressure_2026-06-11.json` vs `pressure_2026-06-10.json`); (4) production spot-check frames 1/125/250 vs approved LRT JPGs: ΔE 0.626/0.624/0.641, gains ≈1.000 — inside the pinned 0.62 (0.58–0.66) post-fix verdict (`tests/fixtures/evidence/seq_spot_postslot3_2026-06-11.json`) |
| shadowwedge near-black chroma lead (ours 0.24 vs libraw 0.03) is the divide-back shim / black-level interaction | **REFUTED 2026-06-11** | falsecolor 0.238 IDENTICAL before/after the shim deletion (pressure re-pin) — the lead is elsewhere (demosaic near-black behaviour or scoring floor); remains on the post-lock queue at low priority (absolute magnitude tiny) |

## Pressure-test articles (the iteration substrate)

| Claim | Status | Regenerate / evidence |
|---|---|---|
| **Epistemic audit (owner challenge): the article suite had partially fallen into the self-labeled-"ground-truth" trap** — the scenes are CONSTRUCTION truth (mosaic contents externally verifiable by any raw reader) but the harness "expected" is internal (our stages 2–9, deliberately, to isolate the front-end) and the original naming overclaimed it | **VERIFIED 2026-06-10 — relabeled + repaired** | Repairs: three-tier epistemics documented in `tools/test_articles/fields.py` + `run_pressure.py`; chart constructions cite external methods (ISO-12233-class bars, Fresnel zone plate); truth-anchored INVARIANTS (chroma where scene is neutral; clip-zone chroma) scored identically for our arms AND independent engines; `dng_validate` caveat recorded (its reference demosaic is BILINEAR — its column ≈ our linear arm by construction, so it anchors colour math, not product-grade edge behaviour) |
| Pressure baseline (articles v1, 2026-06-10): flatpatches all arms ≈ Adobe-ref ≈ 0.15–0.18 ΔE (anchor sane); bars falsecolor — linear 18.9 / rcd 2.2 / menon 1.15 / libraw-AHD 0.88; zoneplate fc — linear 1.04 / menon 0.41 / libraw 0.28 | **VERIFIED 2026-06-10** | `python3 tools/test_articles/make_articles.py && python3 tools/test_articles/run_pressure.py --crops` → `tests/fixtures/evidence/pressure_2026-06-10.json` + labeled 1:1 crops in `~/lrt-cinema-fixtures/test-articles/renders/` |
| **The clip×detail interaction is our dominant front-end defect AND is externally proven avoidable: clipbars falsecolor ours 17.5 (menon AND rcd) vs libraw-AHD 0.88** (unclipped bars: ours 1.15 vs 0.88 — comparable). Mechanism: libraw clips-to-common-white BEFORE demosaic (uniform plateaus); we preserve float headroom through demosaic, so directional interpolators see channel-disparate clip plateaus and invent chroma | **VERIFIED 2026-06-10** | Same evidence JSON, `clipbars` rows. The REVERSAL on smooth ramps closes the design question: clipramp clip-zone chroma — ours 2.85 vs libraw 12.12 (dcraw's classic magenta band). Neither trade is acceptable; darktable's pre-demosaic highlight RECONSTRUCTION addresses both (REFERENCE_PIPELINE TARGET slot 5). This number (clipbars fc 17.5 → ≈1) is the autonomous iteration target for the post-lock fix |

| **Clip-to-common-white SHIPPED as the demosaic highlight DEFAULT (owner-directed 2026-06-10)**: `demosaic_highlights={"clip","headroom"}` on the CFA paths — "clip" (default, dcraw/libraw highlight=0 semantics: scaled mosaic clamped at the minimum multiplier's saturation → uniform plateaus, neutral blown whites on every demosaic) for display targets; "headroom" for the tap-7 scene-linear master (CLI selects by preset). The libraw path already had these semantics post-H1-fix. Documented trade (dcraw's own): >1-multiplier channels lose their top fraction of real highlight detail to the clamp; recovered properly only by future pre-demosaic reconstruction (TARGET slot 5) | **VERIFIED 2026-06-10 — and it WORKS: clipbars menon ΔE 6.03→1.16 (chroma 17.1→1.5, falsecolor 17.5→1.12), ~the unclipped-bars floor; clipramp cost +0.085 ΔE only (clip-zone chroma 3.03 — we did NOT inherit the magenta band and keep the BEST smooth-clip behaviour of all three engines: libraw 12.12, darktable 10.54); clipfield (blown blob) clip-zone 0.01 = libraw parity, dt 1.41. Cross-engine: ours now sits AT libraw's level (0.88) and BEATS darktable's default (13.2) on clipped fine detail** | Suite 578/2 green (stage-7 guard now explicitly requests headroom mode); comprehensive v2 pressure run = `tests/fixtures/evidence/pressure_2026-06-10.json`. Owner rationale on record: "whether we have highlight reconstruction enabled/working, we have a 'clean' pipeline"; owner intuition that the real-image artifacts are clip-related is QUANTITATIVELY SUPPORTED and the remedy is now measured |
| Articles v2 EXPOSED new uncovered defects (the suite working as designed): **diagbars — ALL our arms catastrophic (menon ΔE 16.6, chroma 34)** — diagonal fine detail is the directional-demosaic worst case; **noisebars menon 6.2** (demosaic-on-grain false colour); **clipbars_coolwb 3.38 vs clipbars 1.16** (the develop-WB override regime leaves 3× residual — conditioning matters, H1-regime article works) | **VERIFIED 2026-06-10 (baseline; achievability context = engine falsecolor columns, completed in the final rerun)** | Same evidence JSON. Caveats recorded: the pitch sweep reaches sub-Nyquist frequencies where perfect reproduction is unachievable for ANY demosaic — per-pitch breakdown + engine context decide how much of 16.6 is real headroom vs floor; next-session analysis |
| **Achievability ranking (cross-engine falsecolor invariants, full table): diagbars ours 34.2 vs libraw-AHD 17.3 / dt 20.6 → ~2× REAL headroom (the gap is ours, not physics); noisebars ours 8.0 vs dt 5.1 (FC-suppression class closes it); shadowwedge ours 0.24 vs libraw 0.03 (8× relative, tiny absolute — near-black chroma lead, NEW); clipbars/clipfield/bars/zoneplate/slantededge at or near best-engine level post-fallback** | **VERIFIED 2026-06-10 — the post-lock iteration queue, in order** | Evidence JSON `*_engine_invariants` columns. Post-lock targets: (1) diagbars falsecolor 34→≈17 (demosaic diagonal handling), (2) noisebars 8→≈5 (FC-suppression stage), (3) shadowwedge 0.24→≈0.05 (shadow chroma), all iterated autonomously against the suite, owner eyeballs only verdicts |
| **LR-PRODUCT anchors scored (owner TIFF export of all 11 articles — ACR's shipping front-end on identical inputs): zoneplate 0.02 (vs ours 0.41 — ACR FC-suppression ≈eliminates dense-frequency false colour), diagbars 13.6 (best of all engines; our target sharpens to ≈14), noisebars 4.25, shadowwedge 0.00, clipramp clip-zone 1.07 (real reconstruction wins smooth clips; slot-5 target ≈1). AND TWO PRODUCT-SUPERIOR RESULTS FOR US: bars — ours 1.15 vs LR 2.03; clipbars — ours 1.12 vs LR 3.34 (the owner-directed clip fallback now BEATS the product on the production failure mode)** | **VERIFIED 2026-06-10** | `python3 tools/test_articles/score_lr_anchors.py` (merges `lr_product_invariants` into the evidence JSON); anchors at `~/lrt-cinema-fixtures/test-articles/lr-anchors/*.tif` (re-export only when the article set changes). The post-lock queue is now product-anchored: diagonal demosaic (34→≈14), FC suppression (zoneplate 0.41→≈0.02, noisebars 8→≈4), smooth-clip reconstruction (3.0→≈1.1), near-black chroma (0.24→≈0.0) |
| darktable-cli is a working, pixel-deterministic, product-grade engine anchor on this machine (5.5.0, `/usr/local/bin/darktable-cli`; renders article DNGs in ~5 s; repeated renders pixel-identical, file hashes differ only by embedded timestamps) | **VERIFIED 2026-06-10** | `darktable-cli <article>.dng out.tif --core --disable-opencl` twice + pixel compare. Wired as the third engine arm (invariants-only) in `run_pressure.py` |
| Comprehensive failure-mode taxonomy exists; articles v2 = 11 articles covering front-end rows (diagbars, clipfield, shadowwedge, noisebars, slantededge, clipbars_coolwb added); develop-layer + product-anchor rows designed with status ledger | **VERIFIED 2026-06-10 (coverage ledger, not a completeness proof)** | `tools/test_articles/TAXONOMY.md` — every row carries an article, a design, or a written ✖ justification. Product-grade anchors: dt-cli (wired), LR one-batch export of the article DNGs (STAGED — owner imports `~/lrt-cinema-fixtures/test-articles/*.dng`, exports 16-bit sRGB, no resize, sharpening off → frozen product anchors for every article at once; re-export only when the article set changes) |

| **Slot-5b deciding experiment (suite half) RUN — clean-room opposed (dt/RT's shared algorithm), BOTH placements, menon held constant**: P1 REFUTED (pre-opposed worsens clipramp clip-zone 3.03→3.52 — opposed preserves tint; ACR rolls to neutral); P2 CONFIRMED (post 21.5 > pre 17.2 on clipbars falsecolor — and both ≫ the 5a clip default 1.12: headroom plateaus at detail scale = invented chroma reconstruction cannot undo); P3 split (clipfield pre parity 0.008; post regresses 0.57). **Verdict: 5a clip default STANDS on all product-anchored suite targets; if reconstruction ever ships, mosaic placement dominates RGB placement everywhere measured.** Metric blind spot recorded: articles score vs a sensor-clipped reference → recovered REAL detail scores as error; owner flips decide that half | **VERIFIED 2026-06-11 (pre-registered; suite half). Owner-eyeball half QUEUED** | `python3 tools/hl_reconstruct_experiment.py` → `tests/fixtures/evidence/hl_reconstruct_5b_2026-06-11.json`; native-res flip arms (full production intent, 4016×6016, README'd): `~/lrt-cinema-fixtures/verify-2026-06-11/hl-flip/` (`tools/hl_reconstruct_flips.py`); implementation `src/lrt_cinema/_opposed_reconstruct.py` + `tests/test_opposed_reconstruct.py` (6 contracts incl. exact neutral-case lift) |
| **OWNER EYEBALL VERDICT on slot-6 (rank-1): FC suppression "looks good — visibly reduces the remaining edge artifacts on windows in the gym image"** (fc-flip A/B, native res) | **VERIFIED 2026-06-12 (owner eyes)** | `~/lrt-cinema-fixtures/verify-2026-06-11/fc-flip/`. Default-enable decision still owner's (preset wiring proposal: `--fc-suppress 3` on display targets) |
| **OWNER EYEBALL VERDICT on slot-5b (rank-1): NO visible gain from either reconstruction placement; both CREATE more colour artifacts** ("difficult to judge which is worse by eye") — concordant with the suite verdict (clip 1.12 vs pre 17.2 / post 21.5 on clipbars) | **VERIFIED 2026-06-12 (owner eyes) — the 5b owner half is IN: 5a clip-to-common-white default CONFIRMED from both directions** | `~/lrt-cinema-fixtures/verify-2026-06-11/hl-flip/`. Reconstruction arms remain lab code (`_opposed_reconstruct`), not wired to any default. Owner follow-up: build a DETERMINISTIC reconstruction-quality harness (held-out-truth synthetic clipping) so future parameter work needs no eyes — row below when built |
| **SLOT 5b FORMALLY CLOSED (owner directive 2026-06-12)**: clip-to-common-white = the decided highlight handling; opposed stays lab code (truth harness shows real linear recovery — a future method with competent chroma handling may re-open). Commissioned successors: (1) industry-best reconstruction survey + feasibility assessment (constraints: clean-room/licence, 24 MP×250 runtime, temporal stability for timelapse, numpy/numba); (2) AMaZE-class work APPROVED — phase 0 = score CANONICAL AMaZE via darktable-cli (dt ships AMaZE; sidecar-forced method on our articles) BEFORE the clean-room port, so the port decision rests on measured gains. Vendoring RT's amaze.cc was assessed and REJECTED (rule 6 — GPL in repo) | **DIRECTIVE RECORDED 2026-06-12** | This row gates the next session's work; phase-0 evidence row below when run |
| **Menon is the production CLI demosaic DEFAULT (owner: "fine for the time being")**: `--demosaic` default linear→menon at the CLI layer only; `render_frame` keeps 'linear' so the dng_validate gym gate and library callers are byte-stable. Side effect: the MLX whole-frame fast path (linear-only) no longer engages at defaults — opt back via `--demosaic linear` | **VERIFIED 2026-06-12** | Production spot re-pin with the full new default set (menon + fc3) = row below; suite green |
| **Luminance-led NEUTRAL recovery (survey #1) BUILT + DECIDED: the mechanism works, and its display-path benefit is ZERO by construction on this production — closing the question honestly.** `reconstruct_mosaic_neutral` (opposed's luminance estimate, channel-consistent target, no chrominance tint — the Adobe-documented recovery class). Pre-registered N1–N4: v1 exposed a real design hole (running unclamped reintroduced the clipbars 17.2 explosion — caught by the harness, N2 refuted); v2 = CLAMP-FIRST-THEN-LIFT keeps every 5a guarantee: **all article metrics byte-identical to the clip baseline (zero regression), truth-harness recovery holds (rel_mae 0.186→0.111, N3 ✓), and the real-frame flip is BYTE-IDENTICAL (max diff 0)** — everything recovered sits above the common white, which the display transfer clamps. Concordant with Adobe's own behaviour: ACR recovery engages via negative-Exposure pulls (their doc + Kasson). **Verdict: clip default is exactly optimal for this sequence; neutral recovery is lab code whose value activates on EXR/scene-linear masters or exposure-pulled sequences — revisit when either exists** | **VERIFIED 2026-06-12 (pre-registered, two iterations)** | `python3 tools/hl_neutral_experiment.py` → `tests/fixtures/evidence/hl_neutral_2026-06-12.json`; module addition in `_opposed_reconstruct.py` (clamp-then-lift call convention documented from the v1 lesson); identity-proof flip + README at `~/lrt-cinema-fixtures/verify-2026-06-12/neutral-flip/` |
| **AMaZE PORTED (clean-room, owner GO): the slot-4 diagonal gap is CLOSED BEYOND the product anchor.** Numpy implementation from dt's scalar `amaze.cc` (read-to-learn; algorithm spec + 12-stage structure; translation self-review caught 3 real errors before any test — half-index ±v1 arithmetic = ±2 px, crossed chroma planes, two stencil taps). Article scores through OUR chain: **diagbars 34.2 (menon) → 15.61 (amaze) → 7.22 (amaze + fc-suppress 3) — beats the LR-product anchor 13.6 AND canonical dt-AMaZE-in-dt 9.73**; clipbars falsecolor 0.01 (production failure mode ~eliminated; was 1.12); bars 0.01 / slantededge 0.00 (exact-ratio sweet spot); noisebars 8.39 raw → **4.15 with fc3 (LR anchor 4.25)**; zoneplate 0.42 unchanged (structural, out of demosaic scope per phase 0 — honest). Real-frame sanity: gym amaze-vs-dng_validate mean 0.433 — CLOSER to Adobe than menon's 0.509. Wired: `--demosaic amaze` (display/clip path; headroom keeps menon — AMaZE needs a single uniform clip point and clamps [0,1]). Perf ~0.66 s/MP numpy (≈16 s/frame @24 MP) — numba twin is the scoped follow-up before any default flip | **VERIFIED 2026-06-12 (pre-registered target 34→≈10–17: LANDED at 15.6/7.2)** | `tests/fixtures/evidence/amaze_port_v1_2026-06-12.json` + `amaze_fc3_2026-06-12.json`; module `src/lrt_cinema/_amaze_demosaic.py`; contracts `tests/test_amaze.py` (6); canonical anchor cross-check `dt_amaze_anchor_2026-06-12.json`. Full standings rerun with the amaze arm: row below |
| **Cross-engine canon: how real engines pass HL-reconstruction headroom through demosaic (owner-directed characterization, full-pipeline view).** (1) **RT amaze preserves headroom BY DESIGN**: output is floor-only (`std::max(0.f, 65535.f*…)`, `amaze_demosaic_RT.cc:1531-1577`), input convention 1.0 = MAX-channel white with `clip_pt = 1/initialGain < 1` = min-channel white (`rawimagesource.cc:1454`) — above-clip_pt per-channel headroom flows through (ULIM-bounded) and RT's post-demosaic recovery (`hilite_recon.cc`) CONSUMES it; (2) **the amaze [0,1] output clamp is a DARKTABLE PORT ADDITION** (`_clampnan(…,0,1)`, dt `demosaicing/amaze.cc` tail) which our port faithfully inherited — dt amaze gets `procmin` and NO scaler, so pre-demosaic recovery above min-channel white dies in dt's amaze too; (3) **dt's actual recovery-compatible convention is the normalize→demosaic→denormalize scaler** on its DEFAULT demosaic RCD (`$DEFAULT: DT_IOP_DEMOSAIC_RCD`; scaler at `rcd.c:130,377`, LMMSE same) — opposed/segbased output ("keeps signal scene-referred", highlights.c comment at the processed_maximum update) survives via the scaler; (4) the prior handoff claim "dt feeds reconstructed mosaics into amaze routinely [and ULIM handles it]" is REFUTED as a recovery-preservation claim — the feed exists, the recovery does not survive. **Consequence: a dt-style scaler wrapper around OUR amaze (÷max, clip_pt=wb_min/scale, ×max) is the both-engines-blessed path** — built + verified same day (row below; the smoke numbers reproduced exactly by the checked-in tool) | **VERIFIED 2026-07-07 (local source reads; wrapper evidence row below)** | Sources `~/src-reading/{rawtherapee,darktable}` (shallow @ master) |
| **Scaler-wrapped AMaZE SHIPPED (`amaze_demosaic_headroom`, owner GO 2026-07-07): reconstruction headroom now survives the production demosaic — recovery is display-visible through amaze for the first time.** dt's RCD/LMMSE normalize→demosaic→denormalize convention; the scaled call runs at clip_pt/scale<1 = RT's fielded amaze regime; ported numerics untouched; degenerates BIT-EXACTLY to the direct call on clip-mode input (contract-tested) — every pinned amaze number is byte-stable by construction. Pipeline: `demosaic="amaze"` + `highlights="headroom"` uses the wrapper (the menon fallback + warning deleted). Pre-registered W1–W6 (commit 4fc0534): **W1 ✓ EXACT — wrapped RGB-level truth rel_mae 0.1860/0.0903/0.0749 (clamp/segb/segb_adapt) = the mosaic pins to 4 decimals, direct amaze 0.2676 on all recon arms (recovery destroyed at the port clamp)**; **W2 half-REFUTED, NEW FINDING: fc-suppress 3 on UNBOUNDED headroom RGB makes wrapped clipbars WORSE (segb fc0 17.72 → fc3 27.14; ΔE 6.6→11.4)** — the chroma-median+blur smears large above-white chroma differences into sub-white neighbours at detail pitch; fc3 is safe on clip-path data (≤1) and harmful on headroom recovery data at fine detail → recovery renders need fc0 or a bounded-domain guard (OPEN for the wiring session); W3 ✓ smooth clips land on the menon-segb pins within 0.005 (no amaze-specific pathology); **W4 ✓ CA-before-reconstruction cuts the recovery-added clip-boundary saturation 66 % (proxy 0.098→0.065, clip floor 0.059)** — the owner-flagged saturation-boost mechanism has a working mechanism-level fix (ordering diverges from dt's highlights@4→cacorrect@5; recorded open); W5 ✓ pull-set divergence 122k px>8/255 (clamped-amaze was 13k; menon 112k); W6 split — A 0.5707 / B 0.5730 (north star unmoved), C(CA2) 0.6745 EXPECTED: the LRT reference is CA-uncorrected (XMP census zero CA), so a CA-corrected render must diverge — owner eyes decide. NOTE: B/C flips carry production fc3 — judge the blown windows, not fine texture (the W2 smear class) | **VERIFIED 2026-07-07 (pre-registered before first run, commit 4fc0534; owner-flip half queued)** | `python3 tools/hl_amaze_wrapper_experiment.py` → `tests/fixtures/evidence/hl_amaze_wrapper_2026-07-07.json`; module `_amaze_demosaic.amaze_demosaic_headroom` + 3 contracts in `tests/test_amaze.py`; flips `~/lrt-cinema-fixtures/verify-2026-07-07/amaze-wrapper-flip/` (README'd) |
| **fc-suppress × HL-reconstruction matrix on the PRODUCTION pipeline (task 16): {clip, segb, segb_adapt} × {fc0, fc3} × amaze — the shipped clip+fc3 default CONFIRMED optimal from a third independent direction; the segbased display-path verdict is UNCHANGED but its mechanism under amaze is now measured.** Pre-registered M0–M5: **M0 ✓** the direct construction calibrates (gym clip+fc3 ΔE 0.5707 vs the pinned CLI-amaze+fc3 0.582 / sequence re-pin 0.5708; fc0 0.5999 — fc3 is worth ~0.03 ΔE under amaze); **M1 HALF-REFUTED (the honest headline)**: amaze's final [0,1] output clamp (= common white in balanced units) makes recovery FREE on smooth/large clips — clipramp/clipramp_deep/clipfield article scores byte-identical across all three arms (menon had paid +0.5 clip-zone chroma) — but does NOT neutralize detail-scale clipping: clipbars falsecolor segb+amaze fc0 = **17.71 ≈ menon's 17.16** (channel-disparate above-clip plateaus corrupt sub-white interpolants BEFORE the final clamp; capping ≠ neutralizing); **M2 ✓** fc3 improves-or-holds every article×arm, and cuts the segb clipbars explosion **17.71 → 3.35 (−81 %)** — still ~500× the clip arm's 0.006 (quantifies the owner's "fc3 helps but doesn't remove"); **M3 ✓ EXACT** truth-harness re-pin 0.1860 / 0.0903 / 0.0749 (clamp/segb/segb_adapt; mosaic-level — demosaic/fc axes collapse by construction); **M4 ✓** gym spot |segb−clip| ≤ 0.0003 ΔE at both fc levels (6× block-mean is blind to the pixel-scale mechanism); **M5 ✓ OWNER-CONFIRMED 2026-07-07 (rank-1): "no difference in highlight recovery between any of them"** — exactly the pre-registered outcome; numeric cross-check on the owner's question (export correctness): amaze A-vs-C at −1 EV differs on 13k px >8/255 vs the 07-06 menon set's 112k (8.6×), while the fc0-vs-fc3 control differs on 38 % of pixels — the exports vary, the recovery genuinely does not reach the display. CONSEQUENCE PAIR: the menon clip-edge saturation boost is ABSENT under amaze (segbased artifact-safe on the display path, fine-detail clipping excepted), AND HL reconstruction is INERT by construction there — recovery is reachable only via the headroom path (menon/EXR) or a future non-clamping amaze variant | **VERIFIED 2026-07-07 (pre-registered before first run, commit 47f3e27; M5 owner eyes same day)** | `python3 tools/hl_matrix_experiment.py` → `tests/fixtures/evidence/hl_matrix_2026-07-07.json` (menon cross-refs pinned in-file); flips `~/lrt-cinema-fixtures/verify-2026-07-07/matrix-flip/` (README'd) |
| **PERF MEASUREMENT STANDARD PINNED (owner directive 2026-07-07): all fundamental-speed claims measure SINGLE-WORKER, SINGLE-NUMBA-THREAD CPU (`NUMBA_NUM_THREADS=1`, workers=1, full production elements incl. NEF→DNG convert and TIFF write) — pure algorithm cost, no parallelism. Target ~10× at that standard FIRST; worker/GPU orchestration multipliers (toward 100× net) come only after. No multi-worker vs single-thread comparisons in any perf claim** | **STANDARD RECORDED 2026-07-07** | Baseline ledger = task 18's first deliverable; sequence-wall context: 2.66 s/frame @5 workers / 17.5 s/frame CPU (seq_full_repin) |
| **Round-2 domain probes GENERATED (12 single-variable NEF+XMP pairs)**: Highlights2012 −50/−100 and Shadows2012 +50/+100 (the priority lever: ground truth for the translation work), Contrast ±50, Blacks ±50, Whites ±50 (our impl carries a REFUTED-direction docstring — this probe decides), HSL blue-sat +50, ColorGrade shadow hue/sat. Method = the CAL protocol that pinned Exposure2012 scene-referred (k*=3.992) | **DONE — owner exports received 2026-07-07 and FITTED (rows below)** | `python3 tools/cal_probe_round2.py` → `~/lrt-cinema-fixtures/production/calibration/round2/` (12 owner LR Classic 16-bit sRGB TIFFs now beside the XMPs); fitting harness = `tools/cal_domain_round2.py` |
| **Round-2 DOMAIN/SHAPE FITS RUN (pre-registered P1–P7 in the tool docstring BEFORE any render).** Method per probe: our render vs LR export on the native grid (crop-8 + block-4), ΔE2000 + ΔL/ΔC split + luminance-binned response curves; ARM-D = best GLOBAL display-domain luminance LUT (captures the luminance behaviour of a global op in ANY domain — stages 2–9 are per-pixel, so domains compose; what it cannot capture is chroma treatment + locality); ARM-S = best GLOBAL scene-domain LUT fitted through real pipeline renders (chroma = the domain discriminator, the CALEXP mechanism); LOCALITY = within-bin-centred context correlation of the ARM-D residual at 2 radii, yardstick = the Contrast probes (a true global op reads ~−0.14/0.00 through the base-look floor) | **VERIFIED 2026-07-07** | `python3 tools/cal_domain_round2.py` → `tests/fixtures/evidence/cal_domain_round2_2026-07-07.json` (all curves/LUTs/locality stats; renders cached in `round2/.cal-renders/`, deletable) |
| **Highlights2012 is SCENE-REFERRED + moderately LOCAL** — ours-with-H-dropped ΔE 4.30/8.23 (−50/−100: the lever's size); best global display arm 0.82/1.28 vs best global scene arm 0.55/0.87, and the chroma split is decisive (armS ΔC 0.18/0.26 vs armD 0.65/0.91 — scene application reproduces LR's saturation behaviour); locality real but moderate (active-bin ctx r −0.39/−0.27 vs yardstick −0.14; within-bin resid std 0.10/0.18 vs 0.04). Response fingerprint: monotone pull-down from ~0.005 display lum up, plateau ~1.0/2.2 stops. −100 is SUBLINEAR vs −50 (amp ratio ≈1.8, P1's prediction) | **VERIFIED 2026-07-07 (P1: domain+locality confirmed; ctx-magnitude half was borderline ~2.8× not ≥3×)** | Evidence JSON `probes.CALHIM50/100`; armS scene LUT knots stored per probe |
| **Shadows2012 is STRONGLY LOCAL (the strongest locality on file) + scene-referred at moderate settings** — ctx r24 −0.60/−0.61 (4× yardstick) at BOTH levels; +50: armS 0.47 < armD 0.77 (scene wins, ΔC 0.45 vs 0.71); **+100 DEFEATS every global arm (armD 2.27, armS 4.24)** — the armS failure is a chroma explosion (ΔC 4.79): a global scene-domain lift of ~5 stops amplifies degenerate near-black channel imbalance into casts LR does not show → any implementation MUST be local + near-black-guarded. Response: lift peaks +2.25/+4.95 stops at ~0.0006 display lum, toe pins the deepest blacks (black point stays), fades to zero by ~0.5 lum | **VERIFIED 2026-07-07 (P2 confirmed)** | Same evidence, `probes.CALSH50/100` |
| **Contrast2012 is GLOBAL; the domain lean is DISPLAY (weak); our faithful `apply_contrast_2012` DOES NOT MATCH LR** — locality at the floor (ctx −0.13/+0.00 — this is the yardstick pair); +50: ours 3.26 vs armD 0.81/armS 0.85; **−50: ours 17.23 (ΔL 23!)** — our per-channel law is catastrophically wrong in the negative direction; both global luminance arms leave ΔC ~1.3–1.4 (LR's contrast carries per-channel/saturation behaviour a pure luminance curve cannot express — consistent with a per-channel display S-curve, shape ≠ ours). Response: log S-curve pivot ~0.13 display lum | **VERIFIED 2026-07-07 (P3: global ✓; "our impl within 2×" REFUTED)** | `probes.CALCON50/CALCONM50`. Faithful-contrast recalibration = its own queued lever (probe data on file) |
| **Blacks2012: GLOBAL, scene-referred (decisively at +50)** — armS 0.175 ≈ the base-look floor (armD 0.42); response = shadow-weighted lift (+0.46 st deep, fading by 0.8 lum); ours-as-is +50 ΔE 7.78 → our uniform-bias `apply_blacks_2012` has the wrong shape (P4 confirmed). **−50 (crush): indeterminate** — deep bins crush −6..−8 stops into the black point, armD ≡ armS 0.93 (clipping destroys the discriminator); ours 1.47 | **VERIFIED 2026-07-07** | `probes.CALBL50/CALBLM50` |
| **Whites2012: GLOBAL, small top-weighted curve; +50 BRIGHTENS (Lightroom's direction — measured)** — response +0.27 st peak at 0.36 display lum for +50, mirrored −0.20 for −50; locality at the floor; domain indeterminate at this effect size (armD 0.30/armS 0.34 at +50; armS 0.15/armD 0.24 at −50 — all near the floor). The perceptual `apply_dr_compression` c_top docstring ("+Whites darkens — the inverse of Lightroom") is now MEASURED as inverted vs the product (was REFUTED(code), now REFUTED(measured)) | **VERIFIED 2026-07-07 (P5 confirmed)** | `probes.CALWH50/CALWHM50`. Whites stays dropped+warned on faithful; perceptual keeps the documented-inverted op pending its own calibration |
| **Faithful HSL (`apply_hsl`, Adobe hexcone) VALIDATED AGAINST THE PRODUCT: blue-sat +50 lands ΔE 0.181 (ΔL 0.07 / ΔC 0.15) — at the base-look floor.** First product-anchored validation of a Stage-12 colour op | **VERIFIED 2026-07-07 (P7 half confirmed)** | `probes.CALHSLBLU` |
| **Faithful ColorGrade (`apply_color_grade`, additive split-tone) REFUTED vs the product: shadow hue 240 / sat 40 lands ΔE 17.86 with ΔC 37.4** — our additive law is not LR's Color Grading at all (P7 half refuted). Recalibration = its own queued lever (single-variable probe on file) | **REFUTED 2026-07-07 (measured)** | `probes.CALCGSH` |
| **Highlights/Shadows TRANSLATION SHIPPED (task 19, "the biggest missing lever") — probe-calibrated, applied on EVERY path**: `scene_tone.apply_scene_hlsh` at pipeline slot-7b (balanced scene camera RGB, post scene-EV gain — where the probes measured LR's domain): guided base/detail split on log2 scene luminance (r=192 px, eps=1.0 log2² — swept; locality measurably helps, e.g. HIM100 0.62→0.50) + MEASURED anchor tone tables (armS LUT deltas refined through the op itself, 2 secant iterations vs the LR exports — a parametric smoothstep family was tried and REJECTED for 0.2–0.4-stop shape residual) + near-black chroma roll. **Final validation vs the owner LR exports: H −50 ΔE 0.355 / H −100 0.497 / S +50 0.346 / S +100 0.980 — every anchor beats the best GLOBAL fit arm (0.549/0.865/0.472/2.269) against the ~0.2 base-look floor; the Shadows+100 global-arm failure (4.24) drops 4× under the local op.** Contracts: byte-exact no-op at zero sliders (gym gate re-run green; production H/S=0); both intents (perceptual DR-compression now consumes Whites ONLY — double-application guarded by test); MLX whole-frame raises MlxUnsupported when H/S set (CPU fallback, no silent drop); opposite slider signs mirrored-EXTRAPOLATED (no anchors). Perf: 3.9 s/24 MP frame when active, zero when not. Caveats recorded: single-frame calibration (DSC_4053 gym; content-transfer unvalidated — next probe = a second scene), tables refined at r96/e0.05 then validated at the swept r192/e1.0 (shipped = exactly what was measured). Process: the cal_deflicker `with_suffix` dot-collision bug was REPRODUCED in this harness's first sweep (9 identical results off 2 files — caught, tag-sanitized + asserted, garbage renders deleted) | **VERIFIED 2026-07-07 (pre-registered success bar "≤ armS per anchor": EXCEEDED)** | `python3 tools/cal_hlsh_fit.py --sweep-radius --emit-tables` → `tests/fixtures/evidence/cal_hlsh_fit_2026-07-07.json`; module `src/lrt_cinema/scene_tone.py` (tables pinned in-code); contracts `tests/test_scene_tone.py` (10); owner flips (native-res, README'd): `python3 tools/hlsh_flips.py` → `~/lrt-cinema-fixtures/verify-2026-07-07/hlsh-flip/` — owner verdict IN (row below) |
| **OWNER EYEBALL VERDICT on the round-1 H/S translation (rank-1, 2026-07-08): overall "pretty good", but TWO defect classes REFUTE the round-1 operator core**: (1) Highlights — ours reads as "a linear pulldown of everything above a certain luminance … a flat look" where LR "retains some contrast across the affected region" (LR also shows halo-like interface effects + a sharpened/"clarity" look on recovered highlights the owner calls "odd/weird/fake" — explicitly NOT a target; LR "breaks ugly" at extremes); (2) Shadows +100 — "very weird artifacts": blur-like edges, false-colour patches, an increasingly visible unnatural boundary between black-clipped and lifted-shadow regions. Mechanism diagnosis (code-level): (1) the guided SELF-filter is edge-tracking in high-variance content (a→1 ⇒ base≈signal) so the curve compressed per-pixel local contrast = flat; (2) `log2(lum+1e-6)` deep-toe noise blowups + the binary 2e-4 achromatic gate = the boundary seam. Owner directive: find the state of the art and implement/borrow what is practical | **VERIFIED 2026-07-08 (owner eyes/words) — round-1 guided core SUPERSEDED (v2 row below)** | Flips `verify-2026-07-07/hlsh-flip/`; v2 design + provenance: `src/lrt_cinema/scene_tone.py` + `src/lrt_cinema/_fast_llf.py` docstrings |
| **H/S translation v2 SHIPPED — fast Local-Laplacian tone core (the SOTA class the owner directive asked for), both round-1 defects measurably fixed.** SOTA survey conclusion: the field's two families are edge-aware base/detail (guided/bilateral — what round 1 was) and multiscale pyramid remapping (Local Laplacian, Paris/Hasinoff 2011 + Aubry 2014 fast form — Adobe's own documented class; darktable ships it as the local-contrast local-laplacian mode, `src/common/locallaplacian.c` read-to-learn). v10c's LLF-as-BASE defer does NOT apply (that measured the severed base/detail architecture; this is the integrated form its §3 exempted). v2 core (`_fast_llf.llf_apply_tone`, clean-room from the archived MIT-derived proto): detail arm preserved VERBATIM (|d|≤σr — kills the flat look), edge arm remapped at the tone map's own local slope `1+Δ′(γ)`, absolute calibrated response at the pyramid residual's regional scale (2^L px); fixed γ grid (no per-frame stats — timelapse-stable); **no clarity/detail-boost term by design** (owner: Adobe's sharpened recovered highlights look "odd/weird/fake"). Boundary fixes: toe-floored log channel (max(lum, 1.2e-4) — kills the deep-toe noise blowups; sub-floor pixels inherit their region's ratio → seamless black-clip boundary) + progressive chroma roll (replaces the binary gate). **Measured on the owner's two axes (`tools/hlsh_v2_diagnostics.py`): local-contrast retention vs LR in the Highlights-affected bins 0.95–1.15 (v1: 0.77–0.91 — the flatness, quantified); toe-boundary band C*ab 17.04 ≈ LR 17.14 (v1: 18.61); boundary crops show v1's blotchy lifted patches gone, v2 ≈ LR.** Recalibrated through the v2 op (σr/last-level swept → 2.0/6): final ΔE vs LR exports 0.375/0.581/0.376/1.051 — ΔE-parity with v1 (block-mean ΔE was blind to both defects; the diagnostics axes are the round-2 metric additions), still beats every global arm. Perf 13.5 s/24 MP when active (was 3.9 — LLF cost; zero at zero sliders; numba twin = perf-campaign material). Round-1 evidence preserved separately (refuted-claim rule) | **VERIFIED 2026-07-08 — v2 owner verdict IN (rows below); v2 superseded by v3 same day** | `tests/fixtures/evidence/cal_hlsh_fit_v2_2026-07-08.json`; diagnostics `python3 tools/hlsh_v2_diagnostics.py`; modules `src/lrt_cinema/{_fast_llf,scene_tone}.py`; contracts `tests/test_scene_tone.py` (10) |
| **OWNER VERDICT on v2 (rank-1, 2026-07-08): round-1 artifacts GONE, but a NEW class — HALOS/POOLS**: "odd halo-ing on edges/adjacent-to-adjustment-zones… halos of increased exposure in zones neighboring shadows that were adjusted"; highlight recovery showed "halos/pools of adjustment that stood out significantly"; worst = diag crop1 (bright wall strip inside the lifted curtain region glows; both v1 and v2 bad there). Directive: build a comprehensive artifact detection/measurement suite, run an optimization campaign, lean on the algorithm literature | **VERIFIED 2026-07-08 (owner eyes/words) — v2 L6 residual SUPERSEDED (v3 row below)** | Mechanism: the v2 absolute response lived on the GAUSSIAN pyramid residual at 64 px — a blurred region map that bleeds Δ across strong boundaries (wall inherits curtain lift). Literature corroboration (research workflow, 32/53 claims verify-confirmed): the pool class = the "compartmentalization effect" (Hessel & Morel arXiv:1808.09411 — intensity shift in constant regions, worse for SMALLER regions, measured with bright-squares-on-dark patterns); canonical LLF leaves the residual UNMODIFIED (Aubry 2014); halos peak at INTERMEDIATE pyramid depths and vanish at small/full depths (Aubry 2014 citing Trentacoste). Payload: `tests/fixtures/evidence/llf_halo_research_2026-07-08.json` |
| **HALO ARTIFACT SUITE BUILT + OPTIMIZATION CAMPAIGN RUN (owner directive; pre-registered P1–P5): 25+ arms measured; WINNER (v3) = residual scale L6→L4 (16 px), one constant.** Suite (`tools/hlsh_artifact_suite.py`): synthetic construction-truth articles (textured step edge; bright/dark blobs r16/48/128 — the Hessel-Morel compartmentalization pattern, converged on independently before the research landed; 11.5-stop ramp) + REAL DSC_4053 crops rendered through the actual pipeline vs the LR export anchor; metrics = edge-band halo over/undershoot, blob pool rings, blob interior fidelity, γ-banding, flatness guard, real-crop ΔE + bright-zone glow. Campaign verdicts: P2 ✓ (v2's 16-px blob inherited 2.0 st of region lift = crop1 quantified); P3 ✓ (pure global = zero artifacts, flatness 0.52); **P1 REFUTED** — the guided/joint-bilateral regional map FAILS small features (÷8 map cannot represent them; interior 2.2 st) and worsened real glow; polarity-guard arms REFUTED (region-map contamination → anti-pools, erratic flatness); multi-scale absolute mixed (better synthetic halos, 5× worse real H-glow). The residual-depth sweep IS the Pareto axis — v3/L4: blob interior 2.0→0.57/0.64 st, pool 0.83→0.33, real wall glow +0.32→+0.10 (S) and +6.5→+1.9 (H) L*, flatness 0.97–1.00 held, γ-banding at the pure-global metric floor; Aubry-2014's depth finding independently PREDICTS this axis. Tables recalibrated through the L4 op: final ΔE vs LR exports **0.394/0.605/0.386/1.157** (parity — block-ΔE is blind to the artifact axes; that is WHY the suite exists). crop1 wall glow visually GONE in the v3 arm | **VERIFIED 2026-07-08 — v3 owner verdict IN (row below); v3 SUPERSEDED by v4 same day** | Suite: `python3 tools/hlsh_artifact_suite.py` → `tests/fixtures/evidence/hlsh_artifact_suite_2026-07-08.json` (all arm rows); recal `tests/fixtures/evidence/cal_hlsh_fit_v2_2026-07-08.json`; owner flips `verify-2026-07-08/hlsh-flip-v3/` + boundary crops `verify-2026-07-08/hlsh-v2-diag/` (C arm = v3); measured-but-rejected modes kept in `_fast_llf.llf_apply_tone(absolute_mode=…)` with their evidence |
| **OWNER VERDICT on v3 (rank-1, 2026-07-08): highlights better, but adjusted regions "still look a bit linear (flattened, lack contrast)"; Shadows BAD — lifted levels show artifacts, haloing, and "something that both flattens and blurs the region", worst on the gym bottom (curtain + stage)** | **VERIFIED 2026-07-08 (owner eyes/words) — v3 L4 residual SUPERSEDED (v4 row below)** | Mechanism: the round-2 flatness guard measured only FINE texture (33 px highpass) — blind to the mid-band loss. New per-band metrics (4–256 px) quantify it: v3's 16-px absolute map reads 0.67–0.76 of input contrast in the 32–128 px bands (structure above the map scale follows a BLURRED tone driver = "flattens and blurs"). The scale dilemma is fundamental for single-scale Gaussian maps: Δ(G_L) either follows structure (flattens it, small L) or bleeds across boundaries (pools, large L) |
| **v4 SHIPPED — AMPLITUDE-GATED TWO-SCALE TONE MAP resolves the pool-vs-flatten dilemma; ALL owner axes green simultaneously.** Dead ends measured first (honesty): full-depth canonical arms (residual ≈ global constant, slopes carry tone — the dt architecture) hold per-band contrast 0.97–0.99 but the calibration refit DIVERGES (tables act through their derivative telescoped across levels — the secant rings; the slope family cannot express an arbitrary measured curve: refine0 already 1.2–6.5 ΔE). v4 (`_fast_llf` mode `gate2`): the absolute tone map = COARSE Gaussian chain (64 px) where the fine (8 px) and coarse maps agree within 0.4 st — intra-region structure (folds) keeps its contrast — and the FINE map where they diverge past 1.2 st — region boundaries stay pool-free; smoothstep between. Full-resolution construction (no downsampled-map small-feature failure); response stays a direct table lookup (refit converges: **0.370/0.552/0.381/1.062 — the best anchor set of any version**). Measured on every owner axis: real stage-bottom mid-bands 0.90–0.94 of LR's own band contrast (v3: 0.83–0.85); wall glow S **+0.00**, window glow H **−0.06** L* (v2: +0.38/+6.5; v3: +0.10/+1.9); synthetic plateau bands ≈1.00 through 64 px; flatness 1.00; edge halo 0.05 (below the pure-global metric floor); γ-banding at floor. Known residual flagged for the owner: faint outlining along the very brightest fold ridges in lifted shadows (gate crossover); blob-interior on the extreme 6.5-stop synthetic still 0.48–0.80 st | **VERIFIED 2026-07-08 — v4 owner verdict IN (row below): REFUTED as a Goodhart artifact; SUPERSEDED by v5** | `python3 tools/cal_hlsh_fit.py --emit-tables` → `tests/fixtures/evidence/cal_hlsh_fit_v2_2026-07-08.json`; suite rows (35+ arms) `tests/fixtures/evidence/hlsh_artifact_suite_2026-07-08.json`; owner flips `verify-2026-07-08/hlsh-flip-v4/` + crops `hlsh-v2-diag/` (C arm = v4); module `src/lrt_cinema/_fast_llf.py` mode `gate2` (pinned in `scene_tone.py`: f3, 0.4/1.2 st) |
| **OWNER VERDICT on v4 (rank-1, 2026-07-08): "actually looks worse in most cases … still broken, with an attempted patch/correction on top that creates weirder artifact regions (bright halos that now have dark halos in the middle)". Owner's two-sided diagnosis, both CONFIRMED: (1) the measurement system used regional/local AVERAGES that mask oscillating over/under-corrections — a measurement hack scored as improvement; (2) patch-on-top architecture instead of a fundamental fix** | **VERIFIED 2026-07-08 (owner eyes/words) — the repo's own Goodhart failure mode, caught by rank-1 review** | Instrument repair (same day): structure-sensitive metrics — near-edge profile OSCILLATION count + peak-to-trough SWING, gradient-reversal fraction, per-pixel dL* P95 vs LR, and ours-vs-LR delta-map PNGs (the adjustment field as an inspectable image). VALIDATION AGAINST THE OWNER'S EYES: the repaired instruments rank v4 WORST (stage-crop P95 5.2 vs v2's 4.2 L*; seam swing 2.55 st) exactly where the averaged metrics had ranked it best. Bonus: grad-reversal flags pointwise application of the steep calibrated curve (slope→0 zones flip noise gradients — why per-pixel curves read broken) |
| **v5 SHIPPED — BILATERAL-GRID TONE DRIVER: the fundamental fix (one edge-aware map, no pyramid, no gates, no patches) — every artifact metric at floor AND the best anchors of any architecture.** Also measured + rejected first (honesty): the canonical Paris §5.3 β-edge-compression architecture — β acts on SHARED Laplacian edge coefficients so BOTH sides of an edge move toward each other (real wall glow −2→−8 L* scaling with β), while LR's measured response is one-sided; its pointwise finisher also re-flattens (0.52). v5 (`scene_tone` mode `bilat` + `_fast_llf.bilateral_grid_map`, Chen/Paris/Durand 2007; dt tone-eq's architecture with HDRNet-style grid slicing as the mask producer): out = log_l + Δ(M); M = bilateral-grid-smoothed log2 luminance (spatial σ 96 px, range σ 1.8 st). The RANGE kernel is the region separator: structure < 1.8 st is averaged out of M (folds/texture keep contrast — Δ constant across them, flatness 0.95–1.00); boundaries > 1.8 st never mix (blob interior 0.006 st — reads its OWN luminance; pool 0.025; seam swing 0.00; monotone transitions). Real crops: stage ΔE 1.80 (best measured), P95 4.7-class, wall glow −0.02, all bands 0.83–1.01 of LR. Anchors refit through the op: **0.352/0.527/0.343/0.866** — SH100 under 0.9 for the first time. Perf 4.5 s/24 MP (was 13.5 — no pyramid). Process: the refit initially CACHE-HIT the gate2 renders verbatim (op_hash omitted the mode + bilat knobs — the with_suffix lesson's sibling; caught because the numbers were byte-identical, hash now covers every op-affecting constant) | **VERIFIED 2026-07-08 — OWNER APPROVED (rank-1, 2026-07-08): "Much better. It's not perfect, but actually looks better than LR in some regions. Good enough for now."** The v5 bilateral-grid tone driver is the ACCEPTED H/S translation; the five-version campaign (guided → LLF/L6 → L4 → gate2 → bilat) closes with owner sign-off. Open threads for a future round: "not perfect" residuals unenumerated (owner will flag if they matter); single-frame calibration (second-scene transfer probe still queued); Whites calibration; numba/perf twin if H/S become production-hot | `python3 tools/cal_hlsh_fit.py --emit-tables` → `tests/fixtures/evidence/cal_hlsh_fit_v2_2026-07-08.json`; suite (45+ arm rows incl. structure metrics) `tests/fixtures/evidence/hlsh_artifact_suite_2026-07-08.json`; owner flips `verify-2026-07-08/hlsh-flip-v5/`; delta maps `verify-2026-07-08/hlsh-delta-maps/shipped/`; crops `hlsh-v2-diag/` (C arm = v5) |
| **FULL 250-FRAME SEQUENCE RE-PINNED under the confirmed production defaults (amaze + fc-suppress 3, all slot fixes): mean ΔE vs the approved LRT JPGs = 0.5708 (min 0.532 / max 0.595 / P95 ≈1.2 per frame) — the north-star gap improves from the 0.62 pin to 0.57-class SEQUENCE-WIDE, gains 0.995→0.992 with no 1.0 crossing and weak temporal correlation (+0.21): deflicker/temporal stability intact.** de_old drift (3.3→5.0 vs the pre-overhaul export) is EXPECTED — the old render is superseded, not a target. Perf baseline for the 100× campaign pinned in the same run: 250 frames in 11m05s wall at workers=5 = 2.66 s/frame wall, 17.5 s/frame CPU | **VERIFIED 2026-07-06** | render: CLI defaults over the SanDisk masters (DSC_4053..4302 symlinked); compare: `tools/seq_lrt_compare.py` (validated 8-px-crop + block-6 geometry) → `tests/fixtures/evidence/seq_full_repin_2026-07-06.json` (250 rows). NOTE: the report header's "default linear demosaic" line is the compare tool's stale cosmetic text — the render used the CLI amaze+fc3 defaults (asserted by tests/test_cli.py) |
| **OWNER VERDICT ROUND 2026-07-06 (rank-1, closes three defaults): (1) amaze default CONFIRMED ("Amaze flip: Yes"); (2) fc-suppress 3 default CONFIRMED ("fc3: yes") — and STRENGTHENED: "fc3 looks better in all cases, whether HR is enabled or not, at least on the gym image. It doesn't remove all artifacts, however, but does suppress saturation of them somewhat"; (3) follow-on directives: rigorous fc3×HR matrix on the CURRENT pipeline (amaze), pre-demosaic CA/defringe stage port + test, full-pipeline per-element perf ledger with a 100× net target, EXR gate materials, 250-frame re-pin (SanDisk mounted)** | **VERIFIED 2026-07-06 (owner eyes/words)** | Flip artifacts: `verify-2026-07-06/{amaze-flip,segbased-flip}/`; directives tracked as tasks 15–18 |
| **OWNER EYEBALL VERDICTS 2026-07-06 (rank-1): amaze "controlled clip-edge pixels better than anything else so far"; segbased rebuild "genuinely good at reconstructing large areas of blown highlights — best I've seen so far in this app's development"; BUT segbased B/C arms BOOST SATURATION on the clip-edge (probable lens-CA) pixels vs the clip arm A.** Mechanism (code-level, research-corroborated): the clip arm forces those rims NEUTRAL at common white, while opposed/segbased inpaint chroma FROM the near-clip boundary — and at clipped edges the boundary chroma IS the CA fringe (purple-fringing patents define fringe as saturation-adjacent), so boundary-trusting reconstruction ingests and repaints it. Recognized failure mode across engines (dt #13849 opposed magenta; RT founder Beep6581 on colour propagation: "by definition it propagates colors into clipped areas… [which] suffer from purple fringing"; Zhang&Brainard 2004 magenta-shift + industry desaturate-to-neutral fallback; Adobe rolls neutral). NOT inevitable — control levers: pre-demosaic CA/defringe stage (canon has one; we have NONE — new slot candidate), neutral-chroma rim variant (segbased luminance + `reconstruct_mosaic_neutral` chroma), fc-suppress (the shipped flips LACKED production fc3 — corrected `*_fc3_*` flip set written for re-judging), dt's clip/combine/candidating levers, WB correctness (ours correct by construction). CAVEAT: research fan-out (9 sonnet agents, cross-checked) contains flagged conflicts — RT contributors DISAGREE on colour propagation's merit (Desmis pro, Beep6581 anti); ACR's internal defringe-vs-recovery ORDER is an evidence gap, not a known | **VERIFIED 2026-07-06 (owner eyes + workflow research, claim-vs-claim cross-checked)** | flips `~/lrt-cinema-fixtures/verify-2026-07-06/segbased-flip/` (now incl. fc3 set); research payload: workflow wf_740700d5-ab5 output (71 findings, agreement/conflict analysis); mechanism: `_opposed_reconstruct.py` chrominance-offset term + `_segbased_reconstruct.py` refavg inpainting |
| **dt segmentation-based HL reconstruction PORTED (survey #2, owner-directed) + DECIDED against the deterministic instruments: best LINEAR recovery on file, display default UNCHANGED.** Clean-room `reconstruct_mosaic_segbased` from dt's segbased.c/segmentation.c (read-to-learn; opposed base layer at the SEGMENTS clip magic 0.987 — highlights.c hands opposed's tmpout to the segmentation refiner — + 1/3-res cbrt planes, quasi-disk morphological combine, min-size-4 scan-order segments with border cells, candidate weighting, all-clipped gradient rebuild over an exact EDT). Pre-registered S1–S4: **S1 CONFIRMED — truth-harness rel_mae 0.186 (clamp) → 0.0903 (candidates) → 0.0749 (+rebuild adapt 0.6), beating plain opposed's 0.111 — the best held-out recovery measured in this repo**; **S2 REFUTED honestly — clipbars falsecolor 17.16 ≈ opposed's 17.2: the opposed BASE dominates detail-scale clipping and segment-level candidates cannot fix per-pixel invented chroma**; S3 confirmed (rebuild adds only above-common-white content; article metrics identical to candidates-only). Verdict: 5a clip default remains optimal for the fixed-intent display path (same physics as the neutral-recovery verdict); segbased = the recovery arm for EXR/scene-linear masters or exposure-pulled intent. S4 = owner flips queued, incl. a −1 EV pull set where the rebuild is display-visible. Provenance: adversarial source-review (cheap-model agent, verified in-session) caught 2 real translation bugs pre-ship — border-cell segment membership missing in the full-res candidate lookup, and one spurious radius-8 footprint tap (the reference's `i-w7+6` is a row −7 re-OR) | **VERIFIED 2026-07-06 (pre-registered; owner-eyeball half queued)** | `python3 tools/hl_segbased_experiment.py` → `tests/fixtures/evidence/hl_segbased_2026-07-06.json`; module `src/lrt_cinema/_segbased_reconstruct.py`; contracts `tests/test_segbased.py` (5); flips `~/lrt-cinema-fixtures/verify-2026-07-06/segbased-flip/` (README'd); `reconstruct_mosaic_opposed` gained a `clip_magic` arg (default unchanged) |
| **Slot-2 raw CA correction PORTED (task 17, owner-directed 2026-07-06): clean-room Martinec CA_correct (the dt/RT shared canon — dt's `cacorrect.c` IS RT's `CA_correct_RT.cc` vendored, per its own header), pre-demosaic on the BALANCED mosaic at dt's exact slot (highlights@4 → cacorrect@5 → demosaic@8); opt-in `--ca-correct N` (N = iterations, default 0 = OFF — every existing output byte-identical; `render_frame` default unchanged, gym gate byte-stable). Pre-registered C1–C3 ALL CONFIRMED: C1 synthetic radial CA (the physical magnification model — NB the algorithm's variance gate measures deviation from zero, so spatially-CONSTANT shift is designed-out; the first constant-shift synthetic produced numblox=0, correctly) R/B misalignment cut 82/80 % at α=(+.0025/−.002), 67/61 % at subtle α; C2 CA-free articles NO regression — bars/slantededge metrics unchanged incl. ΔL resolution (G is untouched by construction; the block statistics find no shift and the stage aborts to a no-op), diagbars 8.73→8.65 ΔE / zoneplate 0.489→0.478 marginally IMPROVE, noisebars +0.02 ΔE noise-level; C3 gym DSC_4053 clip-edge fringe ring (3-px band outside the 2-dilated mosaic clip mask, 56,290 px): chroma mean clip arm 4.76→3.44 (−28 %), segbased arm 5.49→3.42 (−38 %), P99 34.6→22.6 — CA-on pulls the segbased arm DOWN TO the clip arm's level, neutralizing the owner-observed reconstruction saturation boost on this metric (the fringe-ingestion mechanism row above). Provenance: translation self-review caught 3 real bugs pre-test (a garbage term in the ceil-row interpolation, reduceat out-of-bounds on edge blocks, an unused-arg typo); adversarial review (cheap-model agent, independent re-transliteration + forced-identical-fitparams pixel diff — 2/176,400 px ≤ 4.4e-4, float-threshold noise) returned 0 bugs / 4 notes, verified in-session: 3 documented as divergences (float64 block stats; RT-precision shift eval; zero-last-pivot abort), 1 false alarm (the avoid-shift guard IS unit-correct). dt-vs-RT divergences adjudicated in the module docstring. Default flip = owner decision (verdict row below) | **VERIFIED 2026-07-07 (pre-registered; owner-eyeball half IN — row below)** | `python3 tools/ca_correct_experiment.py` → `tests/fixtures/evidence/ca_correct_2026-07-07.json`; module `src/lrt_cinema/_ca_correct.py`; contracts `tests/test_ca_correct.py` (10) + CLI default guard in `tests/test_cli.py`; flips (native res, FULL production intent incl. fc3, README'd): `~/lrt-cinema-fixtures/verify-2026-07-07/ca-flip/` (A-clip-ca-off / B-clip-ca-on / C-segb-ca-off / D-segb-ca-on) |
| **OWNER EYEBALL VERDICT on slot-2 CA correction (rank-1): "CA-on is better in all cases"** — across BOTH arm pairs (A/B clip-shipped and C/D segbased) on the native-res, full-production-intent flip set. Both validation halves now concordant: instruments (C1–C3: synthetic 82/80 % recovery, article no-regression, fringe-ring −28/−38 %) AND eyes agree. The default flip (ca_correct 0→2 on display presets) is a separate owner decision — perf context: +~4-6 s/frame CPU numpy (no numba twin yet; task-18 perf standard applies) | **VERIFIED 2026-07-07 (owner eyes/words)** | Flips: `~/lrt-cinema-fixtures/verify-2026-07-07/ca-flip/`; metrics row above (`ca_correct_2026-07-07.json`) |
| **ca-correct = DISPLAY-PRESET DEFAULT (owner-approved 2026-07-07), shipped as 2 iterations + AVOID-SHIFT — the D2 spot gate FAILED honestly and was owner-adjudicated.** D1 exact: ca0 reproduces the pinned amaze+fc3 spots 0.5820/0.5674/0.5791 to the 4th decimal. D2 FAILED for plain ca2: +0.095 ΔE/frame vs the LRT product (5× the pre-registered 0.02 tolerance) — decomposed by a single-frame 3-arm probe: ~half a REAL global R/B cast (gains 0.998/0.994→1.002/0.990; the documented Martinec colour-shift that the references' avoid-shift lever exists to cancel), ~half irreducible reference-defect divergence (the LRT JPG carries UNcorrected lens CA — the same metric-blind-spot class as 5b's "recovered real detail scores as error"). Resolution (owner-picked from the measured arms): avoid-shift ALWAYS ON in the pipeline CA path — cast cancelled (gains within 0.001 of baseline), shipped spots 0.6291/0.6169/0.6285 (+0.049 flat), fringe-ring benefit retained (clip 4.76→3.61 −24 %; segbased 5.49→3.70 −33 %; P99 34.6→23.4); articles stay no-regression class under the shipped config (aborting articles byte-stable; diagbars +0.15 / noisebars +0.14 ΔE, small + recorded). **NORTH-STAR NOTE for future sessions: the 0.57-class spot/sequence pins move to ~0.62-class under this default BY DESIGN** — the +0.049 is divergence from a CA-afflicted reference, NOT regression (rule 3: the rank-1 verdict "CA-on is better in all cases" is the quality authority; a CA-corrected render cannot match a CA-uncorrected reference at fringe pixels). Policy: AUTO = 2 on display presets / 0 on the tap-7 master; explicit flag (incl. 0) wins; avoid_shift hard-on in `_cfa_demosaic` (divergence vs dt's off-default, justified by D2). Perf: 7.8→13.4 s/frame (workers=1 CLI wall; numba twin = task 18). Flips REGENERATED with the shipped avoid-shift arms (B/D) for owner confirmation | **VERIFIED 2026-07-07 (pre-registered gate; failure adjudicated by owner)** | `python3 tools/seq_spot_ca.py` → `tests/fixtures/evidence/seq_spot_ca_2026-07-07.json`; shipped-config experiment rerun → `ca_correct_2026-07-07.json`; defaults `cli.resolve_ca_correct` + guards in `tests/test_cli.py` / `tests/test_ca_correct.py`; flips `verify-2026-07-07/ca-flip/` |
| **OWNER CONFIRMATION on the SHIPPED avoid-shift config (rank-1): "CA on is better in both cases"** — the display-preset default stands as shipped (both flip pairs, avoid-shift arms). NEW rank-1 observation in the same round: the segb+CA arms leave "random 'hot' pixels" in areas where CA is otherwise suppressed; owner asks whether reconstruction-vs-CA ORDERING addresses it. Facts: the shipped/tested order is RECONSTRUCTION → CA (dt's canon: highlights@4 → cacorrect@5); CA-first is the untested reverse — probe row below. The production display path is UNAFFECTED (clip conditioning, no reconstruction; segbased remains lab code for EXR/pulled renders) | **VERIFIED 2026-07-07 (owner eyes/words)** | Flips: `~/lrt-cinema-fixtures/verify-2026-07-07/ca-flip/` |
| **CA-vs-reconstruction ORDER probe (owner question, same day): the metric half does NOT support CA-first.** Pre-registered O1 SPLIT — CA-first removes the single extreme impulse (>15: 1→0) but DOUBLES moderate ring impulses (chroma-vs-3×3-median > 8: 27→50); O2 REFUTED — ring chroma 3.70→3.82 (boundary ingestion is not measurably better addressed at the source); O3 OK — no CA-fit blowup on the unclipped channel-disparate plateaus (the regime dt's canon order avoids). The census is a proxy for the owner's observation — the deciding half is the D-vs-E flip (E = CA-first, added to the flip set). Mechanism note: the hot pixels SURVIVE fc-suppress 3 (a chroma-difference median that never touches G) → they are likely LUMA impulses, which points away from CA ordering and at (a) the canon's **hot-pixel stage — dt ships `hotpixels@6` BETWEEN cacorrect@5 and demosaic@8, a slot we lack entirely** (mosaic-domain impulse filter; targets exactly this class), or (b) masking reconstructed sites out of CA's correction pass, or (c) the segbased rebuild itself creating isolated bright sites that CA-off's larger fringes visually masked. Candidate remedies queued, owner-gated | **VERIFIED 2026-07-07 (pre-registered; owner-eyeball half IN — row below)** | `python3 tools/ca_order_probe.py` → `tests/fixtures/evidence/ca_order_probe_2026-07-07.json`; E flip in `verify-2026-07-07/ca-flip/` (README updated) |
| **OWNER EYEBALL VERDICT on the ORDER probe (rank-1): "CA-first is slightly worse. Definitely doesn't help the region I'm hoping to fix"** — concordant with the probe metrics (impulses 27→50, ring chroma 3.70→3.82). The ordering question is CLOSED: reconstruction → CA (dt's canon order) stands. Owner directive in the same round: port the hotpixels stage (row below) | **VERIFIED 2026-07-07 (owner eyes/words)** | Probe row above; E flip judged |
| **Slot-2.5 hotpixels stage PORTED (owner-directed): clean-room dt `hotpixels` Bayer variant.** Detector: v > threshold with ≥4 (3 with permissive — the adjacent-pair case) same-channel cardinal neighbours below v·strength/2, replaced by the brightest such neighbour; dt defaults strength 0.25 / threshold 0.05. Canon ORDER SPLIT recorded: dt cacorrect@5 → hotpixels@6 (followed); RT interpolates bad pixels BEFORE its CA step. Wired `--hotpixels S` (default 0 = OFF, outputs byte-identical) + `render_frame(hotpixels=)`, CFA paths only, MLX-gate excluded. Provenance: adversarial differential review (cheap-model agent; independent scalar transliteration, 2 000 randomized trials + boundary cases incl. exact-equality ties) — **0 findings, bit-exact**. **HONEST SCOPE RESULT — H1 REFUTED at dt defaults: the stage fires ZERO times on the segb+CA gym mosaic** (strength sweep: 0.25/0.5 strict → 0 fixes; 1.0 strict → 6; 1.0+permissive → 234 of 24 MP) — the detector wants bright-over-dark single-site impulses, and near clip boundaries everything is bright. H2 CONFIRMED: 0 false positives on the bars + zoneplate article mosaics AND the gym clip path — the stage is safe where it fires. Max-reach F flip (1.0+permissive): ring census ~unchanged (chroma 3.703→3.691, impulses 27→30) → the metrics suggest the owner's hot pixels are NOT mosaic-domain single-site impulses; **if the owner's D-vs-F flip agrees, the remedy moves INTO `_segbased_reconstruct` (isolated-candidate/rebuild guard) — next owner-gated item** | **VERIFIED 2026-07-07 (pre-registered; owner verdict row below)** | module `src/lrt_cinema/_hotpixels.py`; contracts `tests/test_hotpixels.py` (8); `python3 tools/ca_order_probe.py` → `ca_order_probe_2026-07-07.json` (incl. the false-positive census); F flip `verify-2026-07-07/ca-flip/` (README'd) |
| **OWNER EYEBALL VERDICT on D-vs-F (rank-1): "no visible difference, artifact isn't this class"** — concordant with the pre-registered prediction (H1 refuted; max-reach ring census unchanged). CONCLUSION, both halves in: the segb hot pixels are NOT mosaic-domain single-site impulses — the CA-ordering AND hotpixels remedies are both eliminated with evidence; the remedy investigation moves INTO `_segbased_reconstruct` (guard against isolated candidate/rebuild sites — the last standing mechanism from the 2026-07-07 candidate list). The hotpixels stage stays as shipped: opt-in, canon-faithful, proven safe where it fires — the correct tool for true sensor hot pixels (long exposures/high ISO), just not for THIS artifact. NEXT OWNER-GATED ITEM: the segbased isolated-site guard | **VERIFIED 2026-07-07 (owner eyes/words)** | F flip judged; hotpixels + probe rows above |
| **Segbased hot-pixel investigation (owner-directed "isolated-site guard"): the guard is BUILT — and the forensics REDIRECTED the diagnosis: the specs are INPUT-level, not reconstruction inventions.** Direct evidence: at all top-15 interior luma impulses in the D render the PEAK value pre-exists in the input mosaic (reconstruction's `max(in, ·)` floor preserves it; 8/15 of the 5×5 windows are fully reconstruction-untouched). Two classes: (a) true sensor hot-pixel PAIRS in unclipped areas — pairs are why dt-hotpixels STRICT mode can never fire (its `permissive` toggle exists for exactly this), and why the earlier post-CA strength sweep found 0 (MEASURED: CA's bilinear R/B resampling smears single-site specs, neutering the 4-neighbour test — RT's bad-pixels-BEFORE-CA order is the working placement, now adopted in `_cfa_demosaic` by measurement); (b) isolated per-channel-ceiling sites in partial-clip zones. The CLIP arm hides both by clamping to common white — headroom/segbased arms expose them. Remedies built: (1) `site_guard` in `reconstruct_mosaic_segbased` — CLAMP clipped sites to 2× their same-channel ring median (census-grounded: ratio P50 1.000 over 95,664 clipped sites, artifact tail 647 > 2×, max 5.8×; the first iteration used median REPLACEMENT and measurably CREATED dark divots — interior impulses 48→129 — caught and fixed to a clamp; truth-harness rel_mae 0.0829→0.0832, +0.4 % rel, safe); (2) `fix_hot_pixels(1.0, permissive)` PRE-reconstruction — 278 fires covering 9/15 spec spots, 0 false positives on bars/zoneplate, truth harness unchanged. **METRIC HONESTY: the interior luma-impulse census does NOT resolve this artifact class** (all arms within ±25 %; it counts recovered fine structure, not the owner's specs) — the deciding instrument is the owner flip set D (baseline) / G (guard) / H (hotpix-permissive → segbased+guard, the input-spec remedy) | **VERIFIED 2026-07-07 (forensics + safety gates; owner verdict row below)** | `python3 tools/segbased_guard_probe.py` → `tests/fixtures/evidence/segbased_guard_2026-07-07.json`; guard `_segbased_reconstruct.py` (`site_guard`, default 0 = byte-stable with all pinned evidence; +4 contracts in `tests/test_segbased.py`); wiring-order fix `pipeline._cfa_demosaic` (hotpixels now precedes CA); flips `verify-2026-07-07/ca-flip/` (README'd) |
| **OWNER EYEBALL VERDICT on D/G/H (rank-1): "H and G look indistinguishable from one another in the regions I'm looking at — both are unambiguously better than D."** The hot-pixel investigation CLOSES with a working remedy: the `site_guard` clamp ALONE fixes the observed artifact — **the segbased-arm recipe is `site_guard=2.0`** (the census metric that could not see this improvement stays retired for this class; eyes decided, as pre-registered). The function default STAYS 0 deliberately: flipping it would silently change the regeneration of every pinned pre-guard segbased evidence row (`hl_segbased_2026-07-06`, the D flips) — future segbased arms pass `site_guard=2.0` explicitly. H's extra hotpixels(permissive, pre-CA) stage added no visible benefit in the judged regions; it retains standing value for TRUE sensor hot pixels in UNCLIPPED areas (5 of the 15 forensic spots — outside the guard's reach and outside the judged regions), available when that class ever shows | **VERIFIED 2026-07-07 (owner eyes/words)** | Flips `verify-2026-07-07/ca-flip/`; investigation row above (`segbased_guard_2026-07-07.json`) |
| **Guard-recipe RE-PIN (owner-directed rerun of the segbased + matrix experiments with `site_guard=2.0`): the recipe costs ~nothing on every instrument.** hl_segbased rerun: truth-harness rel_mae segb 0.0903→0.0906 / segb_adapt 0.0749→0.0752 (+0.3–0.4 % rel — the guard's measured recovery cost); clipbars falsecolor segb arms 17.16→17.68 (the opposed-base detail-scale pathology, unchanged class — the 5a clip display default stays 1.119, untouched). hl_matrix rerun: the gym spot matrix is **IDENTICAL TO 4 DECIMALS on every arm × fc cell** (clip/segb/segb_adapt × fc0/fc3, e.g. segb_fc3 0.5704) — task-16's M-verdicts (fc dominates; reconstruction ~irrelevant at the spot metric) are STABLE under the guard, which is invisible at 6×-block scale (647 sites of 24 MP). Fresh guard-recipe flip sets written (`segbased-flip/`, `matrix-flip/`); the owner-JUDGED pre-guard sets preserved at `*-preguard/`. The pinned task-16 `hl_matrix_2026-07-07.json` is restored byte-identical (same-day filename collision; the reruns live under `-guardrecipe` suffixes) | **VERIFIED 2026-07-07** | `python3 tools/hl_segbased_experiment.py` → `tests/fixtures/evidence/hl_segbased_2026-07-07-guardrecipe.json` (renamed from the tool's dated output); `python3 tools/hl_matrix_experiment.py` → `hl_matrix_2026-07-07-guardrecipe.json` (renamed; the pinned same-name file restored from git) |
| **amaze is the CLI demosaic DEFAULT (decision 2026-07-06, owner-authorized in the "AMaZE efficiency → numba twin → default decision" directive; pre-registered criteria A1–A4 ALL PASS).** A1: the spot construction calibrates exactly — menon+fc3 frame 1 through the REAL CLI reproduces the pinned 0.5865. A2: amaze+fc3 is CLOSER to the LRT product on every spot frame (0.5820/0.5674/0.5791 vs menon 0.5865/0.5722/0.5843; identical gains) — the north-star gap does not regress, it narrows ~0.005. A3: amaze renders FASTER end-to-end (16.5 vs 25.0 s/frame, workers=1 CLI wall). A4: native-res owner flips written (frames 1 + 241, menon vs amaze, full production intent). Frames 1/121/241 used (SanDisk absent; every-10th NEF local; frame 1 = the directly-comparable calibration point). Scope unchanged: display/clip path only — headroom targets fall back to menon (warned); `render_frame` default stays linear (gym gate byte-stable); MLX gate untouched | **VERIFIED 2026-07-06** | `python3 tools/seq_spot_amaze.py` → `tests/fixtures/evidence/seq_spot_amaze_2026-07-06.json`; flips `~/lrt-cinema-fixtures/verify-2026-07-06/amaze-flip/` (README'd); default in `cli.py` (`--demosaic` menon→amaze), assertion in `tests/test_cli.py` |
| **AMaZE numba twin SHIPPED — the 1/50th-runtime directive is MET: 24.3 MP gym frame 18.82 s (numpy, pinned this session) → 0.331 s steady-state = 52.6–56.8×, BIT-EXACT.** Architecture: the numpy twin is the executable spec; the fast twin re-executes the SAME dataflow as ~13 numba prange passes with reflect-band fills at every point the spec pads an intermediate, host-visible global Nyquist gates (numba-ized with exact integer semantics), coset-stride loops for half/quarter-grid quantities, and a per-shape buffer pool (~30 pooled 100 MB planes; pooled reruns bit-stable). **Acceptance: full-frame max\|Δ\| == 0 vs `_amaze_rggb` (borders included, both Nyquist gate paths) + full-chain identity — diagbars/clipbars re-scored through `render_frame(demosaic='amaze')` reproduce the pinned 4-arm evidence to 0.0.** Production dispatch: `amaze_demosaic` → numba when available (`LRT_CINEMA_AMAZE=numpy` forces the spec twin). Debug find recorded: parity initially appeared broken at ~1 ulp on 7 181 px — root cause was the TEST HARNESS (float64 `np.mean` black-level promoted the input mosaic; the numpy twin then computed `green`/`rbint` expressions in double) — the float32-input contract is documented in tests; production entry always casts | **VERIFIED 2026-07-06 (pre-registered target ≤0.376 s: LANDED 0.331 s)** | `python3 tools/amaze_numba_bench.py` → `tests/fixtures/evidence/amaze_numba_2026-07-06.json`; module `src/lrt_cinema/_amaze_numba.py`; parity + pool + dispatcher contracts in `tests/test_amaze.py` (10); stale CLI-default assertion fixed in `tests/test_cli.py` (menon default, owner-approved 2026-06-12) |
| **amaze is a permanent pressure-suite arm (4 arms × 12 articles)** — full standings rerun with `ARMS = (linear, rcd, menon, amaze)`: amaze best-or-tied on every article where demosaic matters (diagbars falsecolor 15.61 vs menon 34.16; diagbars ΔE 8.73 vs 16.58; clipbars falsecolor 0.006 vs 1.12; clipbars_coolwb ΔE 3.02 vs 3.38), zoneplate flat (0.42 ≈ menon 0.41 — structural, as phase 0 predicted). All 12 articles' pre-existing arm metrics reproduced IDENTICALLY (max abs diff 0.0 vs the committed evidence) — the amaze arm addition is regression-free by measurement. RESULTS.md now renders the amaze column in all three tables | **VERIFIED 2026-07-06** | `python3 tools/test_articles/run_pressure.py && python3 tools/test_articles/report_table.py` → `tests/fixtures/evidence/pressure_2026-07-06.json` + `tools/test_articles/RESULTS.md` |
| **HL reconstruction industry survey DELIVERED (owner-commissioned) — headline reframe: Adobe's own whitepaper documents NEUTRAL recovery (1–2-channel survivors return as LUMINANCE, not colour; typical headroom 1–2 stops; independent empirics show true reconstruction is shallow). The competence bar = luminance recovery + neutral chroma + graceful rolloff, NOT chroma resynthesis — concordant with the owner's rank-1 verdicts and our fringe forensics. Ranked shortlist: (1) luminance-led neutral recovery reusing our opposed-luminance + neutral-pull (HIGH feasibility, next experiment); (2) dt segmentation-based clean-room port (~1–2 sessions, the only classical answer to large fully-blown areas); (3) Rouf-style gradient texture continuation (defer). Learned methods DISQUALIFIED for timelapse (per-frame flicker/non-determinism admitted by their own authors; 512² sRGB benchmarks; licence/runtime walls)** | **DELIVERED 2026-06-12** | `docs/HL_RECON_SURVEY.md` (evidence-tiered: [PRIMARY] engine-source reads + Adobe PDF; [BENCH] AIM-2025/arXiv; [DOC]/[PRACT]/[MKT] weighted accordingly). Provenance: research fan-out died twice on token budget (199 agents); outputs salvaged from transcripts locally, verification + synthesis done in-session — process lesson in memory `feedback-subagent-token-budget` |
| **Production spot RE-PINNED under the FULL new default set (menon + fc-suppress 3)**: ΔE 0.587/0.575/0.583 vs the approved JPGs, gains ≈1.000 — identical to fc3-with-linear at the 6×-downsampled metric (demosaic differences are pixel-scale; menon's benefit is the owner-verified native-res artifact reduction, which block-mean ΔE cannot see). The 0.58-class north-star gap (improved from 0.62 by suppression) HOLDS under menon | **VERIFIED 2026-06-12** | `tests/fixtures/evidence/seq_spot_menon_fc3_2026-06-12.json`; full-sequence re-pin queued for the next 250-frame ceremony |
| **AMaZE phase 0 (owner-directed assess-before-port): CANONICAL AMaZE scored via darktable-cli sidecar — diagbars 20.6 → 9.73 (53 % cut, BELOW the LR-product 13.6); the port is GO with anchored payoff. zoneplate hypothesis KILLED: 0.70–0.74 flat across ALL dt demosaic methods (PPG/AMaZE/RCD/LMMSE) — ACR's 0.02 is NOT demosaic-method-class; out of port scope** | **VERIFIED 2026-06-12 (sidecar validated bit-exact; cross-validated)** | `python3 tools/dt_amaze_anchor.py` → `tests/fixtures/evidence/dt_amaze_anchor_2026-06-12.json`. Construction: handcrafted dt XMP (demosaic params v6 struct from local dt source, read-to-learn; AMaZE stays in dt's GPL binary — rule 6 clean). Validation: method=5 sidecar ≡ dt default render at max\|Δ\|=0/65535; dt-rcd diagbars 20.619 ≡ the independent earlier dt arm's 20.6. Side data: bars 1.28→0.61, noisebars 5.07→4.17 (LMMSE 3.33 — noise-best but diagonals-worse 14.6). Port spec: AMaZE-class, expected diagbars ours 34→≈10–14 |
| **clipramp_deep joined the pressure suite + LR-product anchor scored** (12 articles): all three arms rendered; product anchor clip-zone chroma 0.475 / p99 2.15 = the multi-channel staggered-clip target. The 12-article rerun reproduced ALL 11 prior articles' metrics to 1e-9 (regen determinism + slot-3 migration neutrality re-proven end-to-end) | **VERIFIED 2026-06-12** | `tests/fixtures/evidence/pressure_2026-06-11b-clipramp-deep.json` (renamed from the date-collided rerun; the committed `pressure_2026-06-11.json` slot-3 re-pin is restored untouched) + RESULTS.md regenerated |
| **Held-out-truth reconstruction harness BUILT (owner request): reconstruction parameters are now deterministically judgeable — no eyes in the loop.** Construction: (1) real-frame band-clip — gym mosaic ÷0.6 then clamped at 1.0 (the exact physics of a hotter exposure); 97,987 sites carry KNOWN truth (orig ∈ [0.6, 0.99)); (2) analytic articles scored against their UNCLIPPED fields. Scoring in balanced linear camera units at CFA sites; the pressure suite's falsecolor metrics are the display-side complement — a candidate must improve `rel_mae` without regressing falsecolor. First results (pre-registered Q1–Q3): **pre-opposed cuts real-frame recovery error 52 % (rel_mae 0.186→0.090; partial sites −64 %, full-clip interiors unchanged — Q1 ✓ both halves); post ≤ pre everywhere (Q2 ✓); clipramp pre lands 0.004; dilate-parameter sweep measurably separable (d5 0.0870 < d2 0.0905)**. Reconciles the 5b record: opposed genuinely RECOVERS signal in the linear domain while its imperfect chroma at detail scale renders as the visible artifacts the owner (and falsecolor metrics) rejected — the two families bracket the requirement | **VERIFIED 2026-06-12 (pre-registered)** | `python3 tools/hl_truth_harness.py` → `tests/fixtures/evidence/hl_truth_harness_2026-06-12.json`. Bias uniformly negative (opposed never overshoots — `max(in, ·)` floors at the clamp). Deferred + designed in-file: dt-cli cross-anchor (needs WhiteLevel-patched DNG + brightness-matched metric) |
| **Partial-channel-clip physics census (owner challenge)**: the real-frame band-clip clamps per-site at a uniform SENSOR threshold — the sensor's actual saturation behaviour — so partial clips arise naturally with the real ordering (G first: 71.7 % of band sites are G photosites). Population: **60.7 % one-channel-clipped neighbourhoods / 10.9 % two-channel / 28.4 % fully blown**; truth depth to 1.67× (P99 1.64 — used). **Correction (own claim caught): clipramp's analytic partial coverage is G-ONLY** (23.8 % of field clips, R/B never cross as parameterized) — the staggered-multi-channel claim was half-right; analytic 2-channel coverage is a GAP, currently covered only by the real-frame band | **VERIFIED 2026-06-12** | Census snippet on `hl_truth_harness` inputs (same decode path). Dials if needed: SYN_WHITE 0.6→0.4 (2.5× truth depth, more 2/3-ch sites); a steeper-tint `clipramp_deep` article adds analytic 2-channel truth but changes the article set → LR-anchor re-export (batch with the owner's next export) |
| **Truth-harness round 2 (owner-directed): W-sweep + multi-channel analytic article.** SYN_WHITE 0.4 (241,586 sites, truth to 2.5×): clamp 0.286 → pre-opposed-d5 **0.147** (−49 %; partial −67 %); ordering pre < post < clamp ROBUST across depth. NEW `clipramp_deep` article (peak 2.2, tint [1.364, 1.0, 0.703]): analytic G→R→B staggered crossings at 45.5/66.7/83.3 % of ramp — the multi-channel partial-clip regime now has exact truth; pre-opposed −64 % rel_mae (0.284→0.103), post barely beats clamp (0.238). Article generation verified DETERMINISTIC (double-regen hash-stable) → the 11 anchored articles are byte-identical, anchors stay valid; **clipramp_deep needs an LR-product anchor at the owner's next export batch**. Also corrected in-file: clipramp's "R first" comment was FALSE (G-only) | **VERIFIED 2026-06-12** | `python3 tools/hl_truth_harness.py` → `tests/fixtures/evidence/hl_truth_harness_2026-06-12.json`; TAXONOMY.md row added |
| **`--fc-suppress 3` is now the DISPLAY-PRESET DEFAULT (owner-approved post-eyeball)**: AUTO = 3 passes+blur on tone-curved targets, OFF on the scene-linear tap-7 master (EXR stays untouched for grading); explicit flag (incl. 0) wins. `render_frame` itself keeps `fc_suppress=0` — the gym gate, pressure baselines and library callers are byte-stable; the default lives in the CLI preset layer (`cli.resolve_fc_suppress`). **Production spot RE-PINNED with the default ON: ΔE 0.586/0.575/0.581 vs the approved JPGs (was 0.626/0.624/0.641; P95 1.37→1.20) — suppression CLOSES the north-star gap** (ACR bakes equivalent suppression; removing our invented chroma converges toward the product). Sequence-level baselines (0.62 pin) shift accordingly at the next full re-pin. CI guards BOTH paths: preset-default resolution test + a measured on/off effect test (invented chroma must drop ≥25 % on the grain model; G byte-identical) so silent regressions fail the suite | **VERIFIED 2026-06-12** | `python3 -m pytest tests/test_fc_suppress.py` (10 passed); spot evidence `tests/fixtures/evidence/seq_spot_fcdefault_2026-06-12.json` |
| **Slot-6 FC suppression IMPLEMENTED + ITERATED (canon chroma-difference median, clean-room) — noisebars target REACHED, zoneplate honestly out of reach**: passes sweep + RT-blur variant (pre-registered): noisebars falsecolor 7.98→**4.36** @3 passes+blur (LR-product ≈4.25 class; beats dt's 5.1 anchor); diagbars 34.2→23.6 (31 % closed, ΔL unmoved — remainder is demosaic-structural); bars 1.15→1.11 + slantededge IMPROVE (zero resolution cost; G never touched). **P1 REFUTED twice**: zoneplate inert under median (0.410) AND median+blur (0.393) — its invented chroma is low-frequency banding; ACR's 0.02 is demosaic-internal, unreachable for this scheme class. Recommended: 3 passes+blur. Wired `render_frame(fc_suppress=N)` + CLI `--fc-suppress N`, **off by default (owner-gated)**; MLX falls back to CPU when enabled | **VERIFIED 2026-06-11 (pre-registered sweep)** | `python3 tools/fc_suppress_experiment.py` → `tests/fixtures/evidence/fc_suppress_slot6_2026-06-11.json`; module `src/lrt_cinema/_fc_suppress.py` + `tests/test_fc_suppress.py` (8 contracts); owner on/off flip (native-res, README'd): `~/lrt-cinema-fixtures/verify-2026-06-11/fc-flip/` (`tools/fc_suppress_flips.py`) |
| **Slot-4 diagbars remainder (post-suppression 23.6 vs LR 13.6) is interpolation-direction error; libraw alternates disqualified; AMaZE-class port SCOPED (~1–2 sessions), NOT started** | **VERIFIED 2026-06-11 (measurements); scope estimate recorded** | AHD through our balanced chain: diagbars fc 23.8 but bars ΔL 0.39→3.89 (10× structural regression — disqualifying); DCB worse everywhere: `tests/fixtures/evidence/diagbars_libraw_algos_2026-06-11.json`. Anchored payoff of a clean-room AMaZE: diagbars ≈17 (libraw-AHD floor); LR's 13.6 likely needs ACR's co-designed demosaic+suppression. Ledger slot 4 carries the full scope note |
| **Global `Exposure2012` is SCENE-REFERRED (same domain as the mask EVs)** — current post-curve Stage-11 placement FAILS the LR-arbiter (ΔE 2.84/5.85, midtone gain wrong 17 %/53 % at +1/+2 EV); scene-pure-gain and ExposureRamp arms IDENTICAL to 3 decimals, both at the base-look floor (ΔE 0.187/0.287, gain ≤0.05 % off) | **VERIFIED 2026-06-11 (CALEXP probe, owner exports; pre-registered arms)** | `python3 tools/cal_exposure_domain.py` → `tests/fixtures/evidence/cal_exposure_domain_2026-06-11.json`. Pre-registration half-confirmed: A-fails ✓; the predicted B-vs-C highlight split did NOT materialise (recorded honestly — B≡C on this content, so the implementation choice is free: fold into `scene_exposure_ev`). Implementation queued with the slot-3 migration batch; Exposure2012=0 in production → latent |
| **ARCHITECTURE LOCK: TARGET v2 ledger ACCEPTED by owner 2026-06-11 — tentative, evidence-gated** ("It aims us in the right direction, holds decisions open that need further evidence to resolve, locks in what doesn't"). Accepted explicitly: the ledger, the 5b experiment design (owner addendum: **use the best-known current methods for BOTH pre- and post-mosaic reconstruction**), the slot-3 WB-once migration plan, the tentative target. The ARCHITECTURE LOCK GATE row is therefore OPEN for the locked slots; open slots proceed by their written deciding experiments | **VERIFIED 2026-06-11 (owner sign-off)** | `docs/REFERENCE_PIPELINE.md` §TARGET v2. Post-lock autonomous queue + handoff: `~/.claude/plans/lrt-cinema-postlock-handoff-2026-06-11.md` |
| **TARGET v1 sign-off REFUSED by owner (2026-06-11) — for cause**: no per-slot narrative/justification, no references to battle-tested pipelines, no empirical citations, no governing strategy; named warts: WB on BOTH sides of the demosaic left unexplained (verdict: it is a MIGRATION SHIM that preserved downstream interfaces during the H1 hotfix — mathematically telescoping, structurally indefensible as target; TARGET = WB-once, Stage 2 deleted); post-demosaic highlight recon asserted without justification (verdict: canon SPLITS; deciding experiment specified) | **RESOLVED into TARGET v2: the justification ledger** | `docs/REFERENCE_PIPELINE.md` §"TARGET architecture v2" — per-slot references + evidence + verdict + deciding experiment; stated governing strategy; sign-off basis = the ledger, not the diagram. Lock blockers: slot-5b experiment design agreement, slot-3 migration-plan acceptance, owner challenge of the ledger |
| **Comparative-architecture method codified as anti-drift rule 9** (owner: the reference-pipeline skeleton → engine comparison chain made the winning clip default "easy and obvious once we had the right context"; every architectural choice carries stated justification or an explicit plan to obtain it) | **VERIFIED 2026-06-11 (owner-mandated)** | CLAUDE.md rule 9; ledger enforces it |
| Comprehensive engine-vs-engine results table exists as a generated artifact | **VERIFIED 2026-06-11** | `python3 tools/test_articles/report_table.py` → `tools/test_articles/RESULTS.md` (regenerate after every pressure run; renders the evidence JSON: per-article ΔE/ΔL/ΔC arms + five-engine falsecolor + clip-zone invariant standings) |

## Environment & strategy facts

| Claim | Status | Regenerate / evidence |
|---|---|---|
| "No local validation possible: no dng_validate, no DCP, no LR" (old memory) | **REFUTED 2026-06-10** | All three present: `~/lrt-cinema-fixtures/dng_validate`, `/Library/Application Support/Adobe/CameraRaw/CameraProfiles/Camera/Nikon D750/`, `/Applications/Adobe Lightroom Classic` |
| "Perceptual ops still alias faithful" (old memory + cli.py docstring) | **REFUTED** | `develop_ops.py`: `_apply_hsl_perceptual` (OKLCh), `_apply_color_grade_perceptual` (ACEScct CDL), `apply_dr_compression`, `apply_texture_clarity` are real distinct implementations |
| "LRT only provides deflicker" | **REFUTED(owner)** | LRT = per-frame keyframed editing of the full develop-parameter set; no downstream substitute (Resolve keyframes its own grade, not raw develop intent) |
| "No engine reads Adobe crs: intent" | **OVERSTATED** | LRT's own internal export applies simple edits (8-bit JPG, documented inferior — exactly this project's niche) |
| `apply_dr_compression` Whites slider matches LR's direction | **REFUTED(code)** | `develop_ops.py` docstring: "+Whites darkens — the inverse of Lightroom"; constants self-described as "best-effort tuning" → Phase-1c calibration required before any decomposition use |
| ~1.0s/frame render perf (numba) | **VERIFIED 2026-06-10: 1.31 s/frame** | 250 production frames in 5:27 wall (numba, ~7 cores, while other analysis ran). Perf stays on the kill list |
| "RawTherapee macOS CLI is sandbox-broken" (old memory) | **CORROBORATED IN EFFECT 2026-06-10 (mechanism unknown)** | Bundled `rawtherapee-cli` exits 0 emitting nothing and writing nothing (even `--help`/no-args); a signature-stripped user-space copy gets SIGKILLed by Gatekeeper (and triggered the "damaged" dialog; copy deleted, original untouched — its imperfect codesign seal is likely pre-existing, common for RT community builds). Our quarantine hypothesis: REFUTED. RT replication path = GUI session with owner (pp3 params captured) |
| Clean-room RCD provenance (no GPL contamination) | **UNVERIFIED (self-attested)** | One-time spot-check vs RawTherapee `rcd.cc` constants |
| Repo redistributes extracted Adobe profile data (`tests/fixtures/dcp_data/*.npz`) | **RESOLVED 2026-07-07 — PURGED (owner-triggered)**: working tree replaced with a synthetic 90×16×16 fixture (f396a57); full history rewritten with git-filter-repo (all refs, `git log --all -- '*.npz'` = 0), every branch/tag force-pushed; pre-purge backup bundle at `~/lrt-cinema-fixtures/repo-backup-pre-npz-purge-2026-07-07.bundle` (local only, off the remote). Caveat: GitHub server-side unreachable objects persist until their gc — a guaranteed scrub needs a GitHub support ticket; acceptable for a private single-dev repo | Verify: `git log --all -- tests/fixtures/dcp_data`; suite green post-rewrite |
| Owner repeatedly requested an externally-anchored reference pipeline model; **no such artifact was ever produced** | **RESOLVED 2026-06-10** — `docs/REFERENCE_PIPELINE.md` now exists (v2: absolute-position flowcharts, provenance tags, per-op domain audit) | History: grep across all live + archived docs had zero matches for reference-model/ISP-canon content; the internal order audit's F3 correctly flagged the ~3× deflicker under-delivery and was destroyed by B2 |
| **Stale-docs archive audit RAN (owner-sanctioned standing instruction)**: PIPELINE/DECISIONS/VALIDATION/LRT_ROUNDTRIP (~152 KB, all bannered 2026-06-10) moved to `docs/archive/`; live references retargeted; live-docs budget RATCHETED 252,958 → 112,640 bytes (live total measured 93,781) | **VERIFIED 2026-06-11** | `python3 -m pytest tests/test_context_budget.py -q` (3 passed); handoff step A; CLAUDE.md reference-docs section updated |
| **ARCHITECTURE LOCK GATE (owner directive 2026-06-10): pipeline architecture + absolute stage order must be comprehensively locked before ANY further feature work** — incl. enabling highlight reconstruction, EXR tap policy, demosaic default | **ACTIVE GATE** | Owner: "the underlying problem … is likely linked directly to pipeline architecture, order of operations, and how adjustments are applied … needs to be comprehensively locked before moving forward with anything else." Lock procedure + per-op audit table: `docs/REFERENCE_PIPELINE.md`. Every root-caused defect this campaign was an order/domain error (WB-after-demosaic, mask-EV post-curve, MLX tint drop) — the class is real |
| dcraw/LibRaw stage order: `scale_colors` (WB) BEFORE `pre_interpolate`/demosaic; highlight blend/recover POST-demosaic; convert_to_rgb last | **VERIFIED 2026-06-10 (source)** | Read `LibRaw/src/postprocessing/dcraw_process.cpp` (master, via raw.githubusercontent.com) — full call order quoted in REFERENCE_PIPELINE.md |
| darktable v3.0 pipe order: temperature(WB)@3.0 → highlights(recon)@4.0 → demosaic@8.0 → exposure@21.0 (scene-referred) → colorin@28.0 → filmic@46.0 — WB AND highlight-recon PRE-demosaic, exposure far upstream of tone | **VERIFIED 2026-06-10 (source)** | Read `darktable/src/common/iop_order.c` `v30_order` (master, via raw.githubusercontent.com). Strongest single corroboration of the ISP-canon "major adjustments pre-demosaic / exposure scene-referred" structure |
| Our Stage-11/12 develop ops (global Exposure2012, Blacks, Contrast, ToneCurve, HSL, ColorGrade, Sat/Vib) all apply POST-ProfileToneCurve on display-referred data; LR's placement is scene-referred at least for the exposure class | **SUSPECT (class-level; one instance proven)** | The mask-exposure instance is PROVEN (post-curve domain cannot fit LR — CAL evidence); global Exposure2012 is the same machinery class but is zero in production (latent). Per-op audit table + probe design: REFERENCE_PIPELINE.md §"The open architecture question". Probe = CAL-style single-variable XMPs, owner exports |
| Agreement with a single reference (incl. Adobe dng_validate) = correctness | **REFUTED — the trap that hid the WB-ordering bug** | The gym gate certified demosaic-before-WB because Adobe's reference demosaic is insensitive to it (bilinear commutes with per-channel WB; directional algorithms don't). Anti-drift rule added to CLAUDE.md |
| EXR tap-7 master is demonstrably more gradable than the 16-bit TIFF | **UNVERIFIED** | Phase-1f Resolve capability gate; owner judges |
| Stage-7 ">1.0 headroom" on real frames = Stage-2 WB lifting CLIPPED whites (≈ no scene information, all paths) unless `highlight_recovery` reconstructs real estimates. Post-H1-fix the libraw 'linear' path lands blown px at neutral 1.0 BY DESIGN (canonical clip-to-white) — no overrange there; CFA paths (float, no integer cap) still carry it | **VERIFIED 2026-06-10** | `test_stage_7_emission_preserves_more_overrange_than_stage_9` (now rendered on 'rcd'; it FAILED on post-fix 'linear', which is how this was found). Input to the Phase-1f EXR gate: a tap-7 master wanting genuine highlight headroom needs a CFA demosaic + recovery, not bilinear |
| `highlight_recovery` clip DETECTION on the post-fix libraw path is degraded: per-channel clip points moved from 1.0 to `min(wb)/wb_c` (only the min channel, typically G, still trips the 0.99 threshold); CFA paths unaffected (clipped sites still ≈1.0) | **SUSPECT (by construction; not yet measured)** | `highlight_recovery.clip_mask` thresholds at 0.99 absolute. Affects only recovery=on + libraw-algo renders (tap-7 EXR auto-ons recovery; sRGB target defaults off). Fix direction if it matters: per-channel clip levels plumbed from the decode, or CFA demosaic for masters (recommended anyway) |
